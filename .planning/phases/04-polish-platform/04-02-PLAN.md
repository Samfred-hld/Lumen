---
phase: 04-polish-platform
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-03
files_modified:
  - src/pages/Profile.jsx
  - src/components/profile/ProfileSection.jsx
  - src/components/profile/PasswordChangeForm.jsx
  - src/components/profile/EmailUpdateForm.jsx
  - src/components/profile/AccountDeletionDialog.jsx
  - src/components/profile/SessionDisplay.jsx
  - src/App.jsx
  - src/components/Layout.jsx
autonomous: false
requirements:
  - GAPS #14
  - CONCERN #15
  - CONCERN C
must_haves:
  truths:
    - "User can view and edit their profile from a dedicated Profile page"
    - "User can change their password from the Profile page"
    - "User can update their email (with Supabase confirmation flow)"
    - "User can delete their account with confirmation dialog and password re-entry"
    - "User can see their active session (device, last active)"
    - "Profile page is accessible from the sidebar navigation"
  artifacts:
    - path: "src/pages/Profile.jsx"
      provides: "Profile page with account management sections"
    - path: "src/components/profile/PasswordChangeForm.jsx"
      provides: "Password change form with validation"
    - path: "src/components/profile/EmailUpdateForm.jsx"
      provides: "Email update form with confirmation flow"
    - path: "src/components/profile/AccountDeletionDialog.jsx"
      provides: "Account deletion confirmation dialog"
    - path: "src/components/profile/SessionDisplay.jsx"
      provides: "Active session display component"
    - path: "src/components/profile/ProfileSection.jsx"
      provides: "Reusable section wrapper for Profile page"
  key_links:
    - from: "src/pages/Profile.jsx"
      to: "src/lib/AuthContext.jsx"
      via: "useAuth() hook for updateEmail, deleteAccount, getSession"
      pattern: "useAuth"
    - from: "src/components/profile/PasswordChangeForm.jsx"
      to: "src/lib/AuthContext.jsx"
      via: "updatePassword from useAuth()"
      pattern: "updatePassword"
    - from: "src/components/profile/AccountDeletionDialog.jsx"
      to: "src/lib/AuthContext.jsx"
      via: "deleteAccount from useAuth()"
      pattern: "deleteAccount"
    - from: "src/components/Layout.jsx"
      to: "src/pages/Profile.jsx"
      via: "sidebar nav item with Link to /profile"
      pattern: "/profile"
    - from: "src/App.jsx"
      to: "src/pages/Profile.jsx"
      via: "Route element"
      pattern: "Profile"
---

<objective>
Create a Profile page with password change, email update, account deletion, and session display. Add Profile nav item to sidebar and route in App.jsx.

Purpose: Users need to manage their account credentials and see their active sessions. Account deletion with soft delete provides a safe offboarding path.

Output: Complete Profile page with all account management forms, wired into routing and navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-platform/04-CONTEXT.md
@.planning/phases/04-polish-platform/04-UI-SPEC.md
@.planning/phases/04-polish-platform/04-RESEARCH.md
@src/lib/AuthContext.jsx
@src/App.jsx
@src/components/Layout.jsx
@src/components/settings/TabDados.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Profile page components</name>
  <files>src/pages/Profile.jsx, src/components/profile/ProfileSection.jsx, src/components/profile/PasswordChangeForm.jsx, src/components/profile/EmailUpdateForm.jsx, src/components/profile/AccountDeletionDialog.jsx, src/components/profile/SessionDisplay.jsx</files>
  <read_first>
    - src/lib/AuthContext.jsx (must have updateEmail, deleteAccount, getSession from Plan 03 — verify these exist before proceeding)
    - src/pages/Settings.jsx (existing page structure pattern — Section wrapper, form patterns)
    - src/components/settings/TabDados.jsx (existing Section component pattern with icon, title, children, actions)
    - .planning/phases/04-polish-platform/04-UI-SPEC.md (Component Inventory: ProfileSection, PasswordChangeForm, EmailUpdateForm, AccountDeletionDialog, SessionDisplay; Interaction Patterns: Profile Page Layout; Copywriting Contract: CTAs, error states, destructive confirmations; Spacing Scale; Color section for danger zone styling)
    - .planning/phases/04-polish-platform/04-CONTEXT.md (D-05: dedicated Profile.jsx with sidebar nav, D-06: soft delete with 30-day grace + confirmation, D-07: session display via getSession)
  </read_first>
  <action>
    Create `src/components/profile/ProfileSection.jsx`:
    - Reusable section wrapper, similar to TabDados Section component
    - Props: `icon` (Material Symbols name), `title` (string), `children`, `actions` (optional), `variant` (optional: 'default' | 'danger')
    - Default variant: standard card styling with `bg-surface border border-surface-border`
    - Danger variant (for account deletion zone per UI-SPEC): `border-danger/30 bg-danger/5`
    - Section header shows icon + title with `text-display-sm` (22px/700) for title

    Create `src/components/profile/PasswordChangeForm.jsx`:
    - Import `useAuth` from `@/lib/AuthContext`
    - Three fields: current password, new password, confirm new password
    - Use `Input` component from `@/components/ui/input` with `type="password"`
    - Validation: new password minimum 6 characters, confirm must match
    - On submit: call `updatePassword(newPassword)` from useAuth
    - Success: show toast "Senha alterada com sucesso" and clear form
    - Error: show error message from Supabase (e.g., "Não foi possível alterar a senha. Verifique sua senha atual e tente novamente." per UI-SPEC error states)
    - CTA button: "Alterar senha" with `bg-primary` styling per UI-SPEC

    Create `src/components/profile/EmailUpdateForm.jsx`:
    - Import `useAuth` from `@/lib/AuthContext`
    - Show current email (read-only) from `user.email`
    - Input field for new email
    - Validation: valid email format
    - On submit: call `updateEmail(newEmail)` from useAuth
    - Success: show toast "E-mail de confirmação enviado para {newEmail}. Confirme para atualizar." (Supabase sends confirmation to new address)
    - Error: show error message
    - CTA button: "Atualizar e-mail" per UI-SPEC
    - Note below form: "Você receberá um e-mail de confirmação no novo endereço. O e-mail só será alterado após a confirmação."

    Create `src/components/profile/AccountDeletionDialog.jsx`:
    - Import `useAuth` from `@/lib/AuthContext`
    - Uses AlertDialog component from `@/components/ui/alert-dialog` (not Dialog — destructive action needs alert pattern)
    - Trigger: red "Excluir conta" button with danger styling
    - Confirmation text per UI-SPEC (D-06): "Sua conta será desativada por 30 dias. Após esse período, todos os seus dados serão permanentemente excluídos. Digite sua senha para confirmar."
    - Require password re-entry: input field for current password (validate by attempting login or use updatePassword as a check)
    - Actually, for simplicity: just require the user to type "EXCLUIR" in a confirmation input (safer than sending password to Edge Function)
    - On confirm: call `deleteAccount()` from useAuth
    - On error: "Não foi possível excluir a conta. Tente novamente ou entre em contato com o suporte." per UI-SPEC
    - CTA: "Excluir conta" button with `bg-destructive text-destructive-foreground`

    Create `src/components/profile/SessionDisplay.jsx`:
    - Import `useAuth` from `@/lib/AuthContext`
    - Call `getSession()` on mount to get current session info
    - Display: device info (from user agent), last active (from session.expires_at or current time)
    - If no session: show "Nenhuma sessão ativa" per UI-SPEC empty states
    - Use `text-body-lg` for session info, `text-label` for labels

    Create `src/pages/Profile.jsx`:
    - Import all profile components above
    - Import `useAuth` from `@/lib/AuthContext`
    - Page structure per UI-SPEC Interaction Patterns (Profile Page Layout):
      1. Page header: "Perfil" with `text-display-sm` heading
      2. Section "Informações pessoais" (ProfileSection with icon `person`): EmailUpdateForm
      3. Section "Segurança" (ProfileSection with icon `security`): PasswordChangeForm
      4. Section "Sessões" (ProfileSection with icon `devices`): SessionDisplay
      5. Section "Zona de perigo" (ProfileSection with icon `warning`, variant='danger'): AccountDeletionDialog
    - Use `px-lg py-xl` page padding per design system
    - Responsive: stack sections vertically, max-width for readability
  </action>
  <verify>
    <automated>test -f src/pages/Profile.jsx && test -f src/components/profile/PasswordChangeForm.jsx && test -f src/components/profile/EmailUpdateForm.jsx && test -f src/components/profile/AccountDeletionDialog.jsx && test -f src/components/profile/SessionDisplay.jsx && test -f src/components/profile/ProfileSection.jsx && echo "All Profile components created"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/profile/ProfileSection.jsx` exists with icon, title, children props and 'danger' variant
    - `src/components/profile/PasswordChangeForm.jsx` exists with 3 password fields, validation, and updatePassword call
    - `src/components/profile/EmailUpdateForm.jsx` exists with current email display, new email input, and updateEmail call
    - `src/components/profile/AccountDeletionDialog.jsx` exists with AlertDialog, confirmation text per D-06, and deleteAccount call
    - `src/components/profile/SessionDisplay.jsx` exists with getSession() call and session info display
    - `src/pages/Profile.jsx` exists with all 4 sections: Informações pessoais, Segurança, Sessões, Zona de perigo
    - All forms use shadcn/ui components (Input, Button, Label, AlertDialog)
    - All user-facing text is in Brazilian Portuguese per UI-SPEC Copywriting Contract
    - Danger zone uses `border-danger/30 bg-danger/5` styling per UI-SPEC
  </acceptance_criteria>
  <done>Profile page with all account management forms created. All components use shadcn/ui and Portuguese text.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Profile route and sidebar nav item</name>
  <files>src/App.jsx, src/components/Layout.jsx</files>
  <read_first>
    - src/App.jsx (current routes — lines 73-95, protected route pattern)
    - src/components/Layout.jsx (NAV_ITEMS array at line 25-34, sidebar nav rendering at lines 286-320, keyboard shortcut handler at lines 218-238)
    - .planning/phases/04-polish-platform/04-CONTEXT.md (D-05: dedicated Profile.jsx with sidebar nav item)
    - .planning/phases/04-polish-platform/04-UI-SPEC.md (Component Inventory: Layout.jsx changes — add Profile nav item)
  </read_first>
  <action>
    Modify `src/App.jsx`:
    - Add import: `import Profile from './pages/Profile';`
    - Add route inside the protected routes block (after the settings route at line 92):
      `<Route path="/profile" element={<ErrorBoundary><Profile /></ErrorBoundary>} />`

    Modify `src/components/Layout.jsx`:
    - Add Profile to NAV_ITEMS array (after Settings, as the last item):
      `{ path: '/profile', label: 'Perfil', icon: 'person', shortcut: '9' }`
    - Update the keyboard shortcut handler: change `e.key >= '1' && e.key <= '8'` to `e.key >= '1' && e.key <= '9'` so that key '9' navigates to Profile
    - Add a divider or visual separator before the Profile nav item in the sidebar to group it differently (optional — a simple gap or border-top)
    - Do NOT modify the OfflineBar, InstallBanner, or any other changes made by Plan 01
  </action>
  <verify>
    <automated>grep -c "/profile\|Profile" src/App.jsx | grep -v "^0$" && grep -c "perfil\|person\|/profile" src/components/Layout.jsx | grep -v "^0$" && echo "Profile route and nav wired"</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.jsx` imports Profile and has `<Route path="/profile" element={...} />` inside protected routes
    - `src/components/Layout.jsx` NAV_ITEMS includes `{ path: '/profile', label: 'Perfil', icon: 'person', shortcut: '9' }`
    - Keyboard shortcut '9' navigates to /profile
    - Profile nav item appears in the sidebar after Settings
    - Existing nav items and keyboard shortcuts (1-8) work unchanged
  </acceptance_criteria>
  <done>Profile page accessible at /profile with sidebar nav item and keyboard shortcut 9.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify Profile page functionality</name>
  <what-built>Profile page with email update, password change, account deletion dialog, and session display. Sidebar shows Profile nav item.</what-built>
  <how-to-verify>
    1. Navigate to /profile (or click "Perfil" in sidebar)
    2. Verify "Informações pessoais" section shows current email and has "Atualizar e-mail" form
    3. Verify "Segurança" section has password change form with 3 fields
    4. Verify "Sessões" section shows current session info
    5. Verify "Zona de perigo" section has red "Excluir conta" button
    6. Click "Excluir conta" — verify AlertDialog appears with Portuguese confirmation text
    7. Press key '9' — verify navigation to Profile page
    8. Verify sidebar shows Profile nav item with person icon
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase Auth | Password/email changes go through Supabase Auth API |
| client → Edge Function | Account deletion JWT sent to Edge Function |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Spoofing | Email update to wrong address | accept | Supabase sends confirmation to new email; user must click link. Standard flow. |
| T-04-09 | Denial of Service | Account deletion without intent | mitigate | Confirmation dialog requires typing "EXCLUIR" + AlertDialog pattern (D-06) |
| T-04-10 | Information Disclosure | Session display shows device info | accept | Only the authenticated user can see their own session; no PII exposure beyond existing auth |
</threat_model>

<verification>
- Profile page renders at /profile with all 4 sections
- Password change form validates and calls updatePassword
- Email update form calls updateEmail (Supabase sends confirmation)
- Account deletion dialog shows confirmation and calls deleteAccount
- Session display shows current session info
- Sidebar has Profile nav item, keyboard shortcut 9 works
</verification>

<success_criteria>
1. User can navigate to /profile via sidebar or keyboard shortcut 9
2. User can change their password from the Profile page
3. User can update their email (Supabase confirmation flow)
4. User can initiate account deletion with confirmation dialog
5. User can see their active session info
</success_criteria>

<output>
Create `.planning/phases/04-polish-platform/04-02-SUMMARY.md` when done
</output>
