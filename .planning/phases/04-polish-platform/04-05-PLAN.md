---
phase: 04-polish-platform
plan: 05
type: execute
wave: 2
depends_on:
  - 04-04
files_modified:
  - src/lib/backupRestore.js
  - src/components/settings/BackupSection.jsx
  - src/components/settings/RestoreConfirmDialog.jsx
  - src/components/settings/TabDados.jsx
  - src/index.css
  - src/lib/PageNotFound.jsx
  - .github/workflows/ci.yml
  - .env.example
autonomous: false
requirements:
  - GAPS #8
  - GAPS #15
  - GAPS #17
  - GAPS #21
  - CONCERN #14
  - CONCERN #15
  - CONCERN #16
  - CONCERN #17-24
  - CONCERN A
  - CONCERN B
  - CONCERN C
must_haves:
  truths:
    - "User can export all data as a single JSON file"
    - "User can restore from a JSON backup with schema validation and row count summary"
    - "Reports page has a print stylesheet that hides sidebar/nav and shows full-width content"
    - "CI pipeline runs build, lint, and tests on push"
    - "PageNotFound shows Portuguese text instead of English"
    - ".env.example documents all VITE_* variables"
  artifacts:
    - path: "src/lib/backupRestore.js"
      provides: "JSON export/import logic with zod validation"
      exports: ["exportBackup", "importBackup", "validateBackupSchema"]
    - path: "src/components/settings/BackupSection.jsx"
      provides: "Backup/restore UI controls"
    - path: "src/components/settings/RestoreConfirmDialog.jsx"
      provides: "Restore preview and confirmation dialog"
    - path: "src/components/settings/TabDados.jsx"
      provides: "Extended with backup/restore section"
    - path: "src/index.css"
      provides: "Print stylesheet for Reports page"
      contains: "@media print"
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI pipeline"
    - path: "src/lib/PageNotFound.jsx"
      provides: "Portuguese 404 page"
  key_links:
    - from: "src/components/settings/BackupSection.jsx"
      to: "src/lib/backupRestore.js"
      via: "import exportBackup/importBackup"
      pattern: "exportBackup|importBackup"
    - from: "src/components/settings/TabDados.jsx"
      to: "src/components/settings/BackupSection.jsx"
      via: "import and render BackupSection"
      pattern: "BackupSection"
    - from: "src/lib/backupRestore.js"
      to: "src/lib/store"
      via: "imports store functions for data collection"
      pattern: "from.*@/lib/store"
---

<objective>
Add full data backup/restore with JSON export/import and schema validation, add print stylesheet for Reports, set up GitHub Actions CI, fix PageNotFound to Portuguese, and document .env.example.

Purpose: Backup/restore gives users control over their financial data. CI catches regressions. Print stylesheet enables report sharing. Portuguese 404 page maintains language consistency.

Output: BackupSection + RestoreConfirmDialog in TabDados, print CSS, ci.yml, fixed PageNotFound, documented .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-platform/04-CONTEXT.md
@.planning/phases/04-polish-platform/04-RESEARCH.md
@.planning/phases/04-polish-platform/04-UI-SPEC.md
@src/components/settings/TabDados.jsx
@src/lib/store.js
@src/index.css
@src/lib/PageNotFound.jsx
@.env.example
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backup/restore logic and UI components</name>
  <files>src/lib/backupRestore.js, src/components/settings/BackupSection.jsx, src/components/settings/RestoreConfirmDialog.jsx, src/components/settings/TabDados.jsx</files>
  <read_first>
    - src/components/settings/TabDados.jsx (full file — existing handleExportJSON at line 47-51, Section component, props: transactions, budgets, goals, cards)
    - src/lib/store.js (barrel exports — all store functions available for data collection)
    - src/lib/store/settings.js (getExtraCats, getRules, getSalaryConfig, getChangelog, getPaymentMethods, etc.)
    - .planning/phases/04-polish-platform/04-UI-SPEC.md (Interaction Patterns: Backup/Restore section; Copywriting Contract: CTAs "Fazer backup"/"Restaurar dados", destructive confirmations, empty states, error states; Component Inventory: BackupSection, RestoreConfirmDialog)
    - .planning/phases/04-polish-platform/04-CONTEXT.md (D-08: full JSON backup of all entities, D-09: schema validation + row count summary before confirm, D-11: local download only)
    - .planning/phases/04-polish-platform/04-RESEARCH.md (Don't Hand-Roll: use zod for JSON schema validation)
  </read_first>
  <action>
    Create `src/lib/backupRestore.js`:
    - Import zod: `import { z } from 'zod'`
    - Import store functions needed for data collection: `import { getExtraCats, getRules, getSalaryConfig, getChangelog, getPaymentMethods, getCustomPaymentMethods, getFinancings } from '@/lib/store'`
    - Define backup schema using zod:
      ```
      const BackupSchema = z.object({
        version: z.number(),
        exportedAt: z.string(),
        transactions: z.array(z.object({
          id: z.string().optional(),
          date: z.string(),
          description: z.string(),
          type: z.string(),
          value: z.number(),
          category: z.string().optional(),
          paymentMethod: z.string().optional(),
        }).passthrough()), // passthrough allows extra fields
        budgets: z.array(z.object({}).passthrough()),
        goals: z.array(z.object({}).passthrough()),
        cards: z.array(z.object({}).passthrough()),
        categories: z.array(z.string()).optional(),
        rules: z.array(z.object({}).passthrough()).optional(),
        salaryConfig: z.object({}).passthrough().optional(),
      })
      ```
    - Export `exportBackup(transactions, budgets, goals, cards)`:
      - Collect all data: transactions, budgets, goals, cards, plus store functions for categories, rules, salaryConfig, changelog, paymentMethods, financings
      - Structure: `{ version: 1, exportedAt: new Date().toISOString(), transactions, budgets, goals, cards, categories, rules, salaryConfig, changelog, paymentMethods, financings }`
      - Create Blob with `JSON.stringify(data, null, 2)`
      - Trigger download as `lumen_backup_YYYY-MM-DD.json` per existing pattern in TabDados
      - This EXTENDS the existing handleExportJSON in TabDados (same logic, more complete data)

    - Export `validateBackupSchema(jsonString)`:
      - Parse JSON, validate against BackupSchema
      - Return `{ valid: true, data }` or `{ valid: false, error: zodError.message }`

    - Export `importBackup(file)`:
      - Read file as text via FileReader
      - Call `validateBackupSchema(text)`
      - If invalid: return `{ success: false, error: 'Arquivo de backup inválido...' }` per UI-SPEC
      - If valid: return `{ success: true, data, summary: { transactions: N, budgets: N, goals: N, cards: N } }` for the confirmation dialog

    Create `src/components/settings/RestoreConfirmDialog.jsx`:
    - Import Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter from `@/components/ui/dialog`
    - Import Button from `@/components/ui/button`
    - Props: `open`, `onClose`, `summary` (row counts), `onConfirm`
    - Show summary per D-09: "N transações, N orçamentos, N metas, N cartões serão importados."
    - Warning text: "Isso substituirá todos os seus dados atuais. Esta ação não pode ser desfeita." per UI-SPEC destructive confirmations
    - Two buttons: "Cancelar" (outline) and "Restaurar dados" (destructive/red)
    - On confirm: call `onConfirm()`

    Create `src/components/settings/BackupSection.jsx`:
    - Import `exportBackup`, `importBackup` from `@/lib/backupRestore`
    - Import `RestoreConfirmDialog` from `@/components/settings/RestoreConfirmDialog`
    - Import Button, Input from shadcn/ui
    - Props: `transactions, budgets, goals, cards` (same as TabDados)
    - Two actions:
      1. "Fazer backup" button: calls `exportBackup(transactions, budgets, goals, cards)`, shows success toast
      2. "Restaurar dados" button: opens file picker (accept=".json"), calls `importBackup(file)`, shows RestoreConfirmDialog with summary
    - On restore confirm: write imported data to Supabase tables via store functions (batch insert transactions, budgets, goals, cards). Show progress. On complete: show success toast and refresh page.
    - Empty state (no data): show "Nenhum backup encontrado" message per UI-SPEC

    Modify `src/components/settings/TabDados.jsx`:
    - Import `BackupSection` from `@/components/settings/BackupSection`
    - Add a new Section below the existing export section (or replace the existing JSON export button):
      - Section with icon `backup`, title "Backup e Restauração"
      - Render `<BackupSection transactions={transactions} budgets={budgets} goals={goals} cards={cards} />`
    - Keep existing CSV and PDF export buttons as-is
    - The existing `handleExportJSON` function can be removed or kept as a legacy fallback — BackupSection now handles JSON export with more complete data
  </action>
  <verify>
    <automated>test -f src/lib/backupRestore.js && test -f src/components/settings/BackupSection.jsx && test -f src/components/settings/RestoreConfirmDialog.jsx && grep -c "BackupSection" src/components/settings/TabDados.jsx | grep -v "^0$" && echo "Backup/restore created and wired"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/backupRestore.js` exports `exportBackup`, `importBackup`, `validateBackupSchema`
    - `exportBackup` creates a JSON file with all entities (transactions, budgets, goals, cards, categories, rules, salaryConfig, changelog, paymentMethods, financings)
    - `validateBackupSchema` uses zod to validate backup structure
    - `importBackup` returns summary with row counts for confirmation dialog
    - `src/components/settings/BackupSection.jsx` has "Fazer backup" and "Restaurar dados" buttons per UI-SPEC
    - `src/components/settings/RestoreConfirmDialog.jsx` shows row count summary and destructive confirmation per D-09
    - `src/components/settings/TabDados.jsx` renders BackupSection in a new "Backup e Restauração" section
    - All text in Brazilian Portuguese per UI-SPEC Copywriting Contract
  </acceptance_criteria>
  <done>Backup/restore with JSON export, zod validation, row count summary, and confirmation dialog. Integrated into TabDados.</done>
</task>

<task type="auto">
  <name>Task 2: Add print stylesheet, fix PageNotFound, document .env.example</name>
  <files>src/index.css, src/lib/PageNotFound.jsx, .env.example</files>
  <read_first>
    - src/index.css (full file — find the end of the file to add @media print block)
    - src/lib/PageNotFound.jsx (full file — English text: "Page Not Found", "Go Home", admin note)
    - .env.example (current content — only has VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY)
    - .planning/phases/04-polish-platform/04-UI-SPEC.md (Component Inventory: Print Stylesheet section; Copywriting Contract for print header)
    - .planning/phases/04-polish-platform/04-CONTEXT.md (D-15: @media print on Reports page only; D-14: document all VITE_* vars)
  </read_first>
  <action>
    Modify `src/index.css` — add print stylesheet at the end of the file per D-15:
    ```css
    /* ═══ Print Stylesheet — Reports Page ═══ */
    @media print {
      /* Hide interactive/navigation elements */
      aside, /* sidebar */
      nav, /* bottom nav */
      .fixed, /* FAB, modals, floating elements */
      [role="dialog"],
      button:not(.print-keep) {
        display: none !important;
      }

      /* Full-width content */
      body {
        overflow: visible;
      }

      main, .flex-1 {
        width: 100% !important;
        max-width: 100% !important;
        margin: 0 !important;
        padding: 0 !important;
      }

      /* Print header */
      .print-header::before {
        content: "Relatório gerado pelo Lúmen — " attr(data-date);
        display: block;
        font-size: 10px;
        color: #64748B;
        margin-bottom: 16px;
        padding-bottom: 8px;
        border-bottom: 1px solid #E2E8F0;
      }

      /* Ensure charts render */
      canvas, svg {
        max-width: 100% !important;
        page-break-inside: avoid;
      }

      /* Page breaks */
      .page-break-before {
        page-break-before: always;
      }

      /* Avoid orphaned headings */
      h1, h2, h3 {
        page-break-after: avoid;
      }
    }
    ```

    Modify `src/lib/PageNotFound.jsx` — translate to Portuguese per CONCERN A and CONCERN C:
    - Line 35: "Page Not Found" → "Página não encontrada"
    - Line 38: "The page ... could not be found in this application." → "A página \"{pageName}\" não foi encontrada nesta aplicação."
    - Line 50: "Admin Note" → "Nota do administrador"
    - Line 52: "This could mean that the AI hasn't implemented this page yet. Ask it to implement it in the chat." → "Isso pode significar que esta página ainda não foi implementada. Solicite a implementação no chat."
    - Line 69: "Go Home" → "Voltar ao início"

    Modify `.env.example` per D-14:
    - Keep existing vars but add descriptions:
    ```
    # Lúmen — Environment Variables
    # Copy to .env.local and fill in your Supabase project values

    # Supabase project URL (from Supabase Dashboard → Settings → API)
    VITE_SUPABASE_URL=https://chjndtzrljiywzynrgrb.supabase.co

    # Supabase anonymous/public key (from Supabase Dashboard → Settings → API → anon public)
    VITE_SUPABASE_ANON_KEY=your_anon_key_here
    ```
    - These are the only VITE_* vars in the project. No additional vars needed for PWA (vite-plugin-pwa config is in vite.config.js, not env vars).
  </action>
  <verify>
    <automated>grep -c "@media print" src/index.css | grep -v "^0$" && grep -c "Página não encontrada\|Voltar ao início" src/lib/PageNotFound.jsx | grep -v "^0$" && grep -c "Supabase project URL\|Supabase anonymous" .env.example | grep -v "^0$" && echo "Print CSS, PageNotFound, .env.example done"</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.css` contains `@media print` block that hides sidebar, nav, FAB, and dialog elements
    - Print stylesheet ensures full-width content and page-break handling for charts/headings
    - `src/lib/PageNotFound.jsx` text is entirely in Brazilian Portuguese: "Página não encontrada", "Voltar ao início", "Nota do administrador"
    - `.env.example` has descriptive comments for each VITE_* variable explaining where to find the value
    - No new VITE_* variables added (PWA config is in vite.config.js)
  </acceptance_criteria>
  <done>Print stylesheet for Reports page, PageNotFound translated to Portuguese, .env.example documented with descriptions.</done>
</task>

<task type="auto">
  <name>Task 3: Create GitHub Actions CI pipeline</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - package.json (scripts: build, lint, test, typecheck, build:check — lines 6-21)
    - .planning/phases/04-polish-platform/04-CONTEXT.md (D-12: GitHub Actions .github/workflows/ci.yml; D-13: Lighthouse in CI)
    - .planning/phases/04-polish-platform/04-RESEARCH.md (Environment Availability: Node v24.14.0, npm 11.14.1)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` per D-12:

    The workflow should:
    - Trigger on: `push` to `main` and `pull_request` to `main`
    - Use `ubuntu-latest` runner
    - Steps:
      1. Checkout code
      2. Setup Node.js (version 20 — LTS, stable)
      3. Install dependencies: `npm ci`
      4. Run lint: `npm run lint`
      5. Run tests: `npm test`
      6. Run typecheck: `npm run typecheck`
      7. Run build: `npm run build`
      8. (Optional) Lighthouse CI audit per D-13:
         - Install `@lhci/cli` as a dev dependency or use `npx @lhci/cli autorun`
         - Add a basic `lighthouserc.json` config or inline config for PWA and accessibility audits
         - This step can be marked as `continue-on-error: true` initially since it requires a running server

    Keep the workflow simple and focused. The `build:check` script already runs build + typecheck + lint + test, so alternatively just run `npm run build:check` as a single step.

    Note: The `.github/workflows/` directory does not exist yet — create it.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -c "npm.*build\|npm.*test\|npm.*lint" .github/workflows/ci.yml | grep -v "^0$" && echo "CI pipeline created"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists
    - Workflow triggers on push to main and PR to main
    - Workflow runs lint, test, typecheck, and build steps
    - Uses Node.js 20 (LTS)
    - Uses `npm ci` for dependency installation
    - All steps are non-interactive (no user input required)
  </acceptance_criteria>
  <done>GitHub Actions CI pipeline runs lint, test, typecheck, and build on every push/PR to main.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Verify backup/restore and print layout</name>
  <what-built>Backup/restore in Settings > Dados, print stylesheet for Reports, Portuguese 404 page, CI workflow, documented .env.example.</what-built>
  <how-to-verify>
    1. Go to Settings > Dados tab — verify "Backup e Restauração" section with "Fazer backup" and "Restaurar dados" buttons
    2. Click "Fazer backup" — verify JSON file downloads with all entities
    3. Click "Restaurar dados" — select a JSON file — verify confirmation dialog shows row counts
    4. Navigate to /reports — press Ctrl+P — verify sidebar and nav are hidden in print preview, content is full-width
    5. Navigate to /nonexistent — verify page shows "Página não encontrada" in Portuguese
    6. Check .github/workflows/ci.yml exists with build/test/lint steps
    7. Check .env.example has descriptive comments for each variable
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file → app (restore) | Uploaded JSON file could contain malicious data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | Malicious backup file | mitigate | zod schema validation on import; passthrough allows extra fields but required fields are validated |
| T-04-12 | Information Disclosure | Backup file contains all financial data | accept | D-11: backup is local download only, no cloud upload. User controls file storage. |
| T-04-13 | Denial of Service | Restore overwrites all data | mitigate | D-09: confirmation dialog shows row counts before import; destructive action warning |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Only new package is vite-plugin-pwa (Plan 01), verified [OK] in RESEARCH.md |
</threat_model>

<verification>
- Backup exports JSON with all entities
- Restore validates schema with zod and shows row count summary
- Print preview hides sidebar/nav and shows full-width Reports content
- PageNotFound shows Portuguese text
- CI workflow runs on push/PR
- .env.example has descriptive comments
</verification>

<success_criteria>
1. User can export all data as JSON and restore from it with validation
2. Print preview of Reports page shows clean layout without navigation
3. 404 page displays Portuguese text
4. GitHub Actions CI runs build, test, lint on push/PR
5. .env.example documents all environment variables with descriptions
</success_criteria>

<output>
Create `.planning/phases/04-polish-platform/04-05-SUMMARY.md` when done
</output>
