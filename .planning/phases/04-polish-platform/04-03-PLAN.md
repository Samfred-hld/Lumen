---
phase: 04-polish-platform
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/AuthContext.jsx
  - supabase/functions/delete-account/index.ts
autonomous: true
requirements:
  - GAPS #14
  - CONCERN #15
must_haves:
  truths:
    - "User can delete their account with confirmation and password re-entry"
    - "Account deletion uses a Supabase Edge Function (service_role key never exposed to client)"
    - "AuthContext exposes updateEmail and deleteAccount methods"
  artifacts:
    - path: "supabase/functions/delete-account/index.ts"
      provides: "Edge Function for secure account deletion"
      exports: ["serve"]
    - path: "src/lib/AuthContext.jsx"
      provides: "Auth context with updateEmail and deleteAccount methods"
      exports: ["AuthProvider", "useAuth"]
  key_links:
    - from: "src/lib/AuthContext.jsx"
      to: "supabase/functions/delete-account"
      via: "supabase.functions.invoke('delete-account')"
      pattern: "functions.invoke.*delete-account"
    - from: "supabase/functions/delete-account/index.ts"
      to: "supabase.auth.admin.deleteUser"
      via: "admin API call with service_role key"
      pattern: "admin.deleteUser"
---

<objective>
Add account deletion via Supabase Edge Function and extend AuthContext with updateEmail and deleteAccount methods.

Purpose: Account deletion requires the service_role key which must never be in client code. The Edge Function handles this securely. updateEmail uses Supabase's built-in confirmation flow.

Output: Working Edge Function for account deletion, AuthContext with updateEmail and deleteAccount methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-platform/04-CONTEXT.md
@.planning/phases/04-polish-platform/04-RESEARCH.md
@src/lib/AuthContext.jsx
@src/api/supabaseClient.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create delete-account Supabase Edge Function</name>
  <files>supabase/functions/delete-account/index.ts</files>
  <read_first>
    - src/lib/AuthContext.jsx (current auth methods — login, signup, logout, resetPassword, updatePassword)
    - src/api/supabaseClient.js (supabase client initialization)
    - .planning/phases/04-polish-platform/04-RESEARCH.md (Pattern 3: Supabase Auth Account Management, Pitfall 3: Account Deletion Without Edge Function)
    - .planning/phases/04-polish-platform/04-CONTEXT.md (D-06: soft delete with 30-day grace period + confirmation dialog)
  </read_first>
  <action>
    Create the Supabase Edge Function at `supabase/functions/delete-account/index.ts`:

    The function must:
    - Extract the JWT from the `Authorization: Bearer <token>` header
    - Validate the JWT by calling `supabase.auth.getUser(token)` — reject with 401 if invalid
    - Call `supabase.auth.admin.deleteUser(user.id, true)` for soft delete per D-06 (second argument `true` = soft delete with 30-day grace period)
    - Return `{ success: true }` on 200, or `{ error: message }` on failure
    - Use Deno runtime (Supabase Edge Functions standard): `import { serve } from 'https://deno.land/std@0.177.0/http/server.ts'`
    - Create Supabase client with `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` from `Deno.env`

    Important security constraint: The `SUPABASE_SERVICE_ROLE_KEY` must NEVER appear in client-side code. It is only available in the Edge Function runtime.

    Note: The Edge Function cannot be tested locally without Supabase CLI + Docker. The function code should be written correctly based on the Supabase documentation pattern. Deployment to Supabase is done via the Supabase Dashboard UI or CLI (not in this task).

    CORS headers: Include standard CORS headers for the function since it's called from the browser:
    - `Access-Control-Allow-Origin: *`
    - `Access-Control-Allow-Headers: authorization, x-client-info, apikey, content-type`
  </action>
  <verify>
    <automated>test -f supabase/functions/delete-account/index.ts && grep -c "admin.deleteUser\|getUser\|serve" supabase/functions/delete-account/index.ts | grep -v "^0$" && echo "Edge Function created"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `supabase/functions/delete-account/index.ts`
    - Function imports `serve` from Deno std and `createClient` from `@supabase/supabase-js`
    - Function extracts JWT from `Authorization` header
    - Function validates user via `supabase.auth.getUser(token)`
    - Function calls `supabase.auth.admin.deleteUser(user.id, true)` for soft delete per D-06
    - Function returns 401 for invalid tokens, 200 for success, 500 for errors
    - `SUPABASE_SERVICE_ROLE_KEY` is read from `Deno.env`, never hardcoded
    - CORS headers are included in all responses
  </acceptance_criteria>
  <done>Edge Function code created at supabase/functions/delete-account/index.ts with proper auth validation, soft delete, and CORS support.</done>
</task>

<task type="auto">
  <name>Task 2: Extend AuthContext with updateEmail and deleteAccount methods</name>
  <files>src/lib/AuthContext.jsx</files>
  <read_first>
    - src/lib/AuthContext.jsx (full file — current methods: login, signup, logout, resetPassword, updatePassword)
    - src/api/supabaseClient.js (supabase client for functions.invoke)
    - .planning/phases/04-polish-platform/04-RESEARCH.md (Pattern 3: Supabase Auth Account Management — updateEmail and deleteAccount patterns)
    - .planning/phases/04-polish-platform/04-CONTEXT.md (D-06: account deletion, D-07: session display)
  </read_first>
  <action>
    Add two new methods to `src/lib/AuthContext.jsx`:

    1. `updateEmail(newEmail)`:
       - Calls `supabase.auth.updateUser({ email: newEmail })`
       - Supabase sends a confirmation email to the new address — the email is NOT changed until the user clicks the confirmation link
       - Returns the data on success, throws error on failure
       - Add to the context provider value

    2. `deleteAccount()`:
       - Gets the current session via `const { data: { session } } = await supabase.auth.getSession()`
       - Calls `supabase.functions.invoke('delete-account', { headers: { Authorization: \`Bearer \${session.access_token}\` } })`
       - If the Edge Function returns an error, throw it
       - On success, call `logout()` to clear local state
       - Add to the context provider value

    3. `getSession()`:
       - Returns `supabase.auth.getSession()` result — needed by Profile page for session display per D-07
       - Add to the context provider value

    Also add `updateEmail`, `deleteAccount`, `getSession` to the provider's value object alongside existing methods.

    Do NOT modify any existing methods (login, signup, logout, resetPassword, updatePassword).
  </action>
  <verify>
    <automated>grep -c "updateEmail\|deleteAccount\|getSession" src/lib/AuthContext.jsx | grep -v "^0$" && echo "Auth methods added"</automated>
  </verify>
  <acceptance_criteria>
    - `updateEmail` method exists and calls `supabase.auth.updateUser({ email })` 
    - `deleteAccount` method exists and calls `supabase.functions.invoke('delete-account', ...)` with auth header
    - `deleteAccount` calls `logout()` on success to clear local state
    - `getSession` method exists and returns `supabase.auth.getSession()` result
    - All three methods are included in the AuthContext.Provider value
    - Existing methods (login, signup, logout, resetPassword, updatePassword) are unchanged
    - `useAuth()` hook returns all new methods
  </acceptance_criteria>
  <done>AuthContext extended with updateEmail, deleteAccount, and getSession methods. All existing auth functionality preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Edge Function | JWT sent over HTTPS, validated server-side |
| Edge Function → Supabase Auth | service_role key used only server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Elevation of Privilege | service_role key exposure | mitigate | Key stored only in Supabase Edge Function secrets, never in client code or .env |
| T-04-05 | Denial of Service | Account deletion without confirmation | mitigate | D-06 requires confirmation dialog + password re-entry before calling deleteAccount (implemented in Plan 02 Profile UI) |
| T-04-06 | Spoofing | updateEmail to wrong address | accept | Supabase sends confirmation to new email; user must click link to confirm |
</threat_model>

<verification>
- Edge Function file exists with correct Supabase auth patterns
- AuthContext exports updateEmail, deleteAccount, getSession
- No service_role key in client-side code
- Existing auth methods unchanged
</verification>

<success_criteria>
1. `supabase/functions/delete-account/index.ts` exists with proper auth validation and admin.deleteUser call
2. AuthContext exposes `updateEmail`, `deleteAccount`, `getSession` via useAuth() hook
3. No service_role key appears in any client-side file
4. Existing auth flow (login, signup, logout, password reset) works unchanged
</success_criteria>

<output>
Create `.planning/phases/04-polish-platform/04-03-SUMMARY.md` when done
</output>
