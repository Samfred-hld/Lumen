---
phase: 03-feature-gaps
plan: 05
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
  - "03-04"
files_modified:
  - src/pages/Recurring.jsx
  - src/App.jsx
  - src/components/Layout.jsx
autonomous: true
requirements:
  - "GAPS #2"

must_haves:
  truths:
    - "User can view, create, edit, toggle, and delete recurring transactions from the UI"
    - "Recurring transactions page is accessible from sidebar navigation"
    - "Recurring transactions route is registered in the app router"
  artifacts:
    - path: "src/pages/Recurring.jsx"
      provides: "Recurring transactions management page"
      contains: "Recurring"
    - path: "src/App.jsx"
      provides: "Route for /recurring"
      contains: "/recurring"
    - path: "src/components/Layout.jsx"
      provides: "Sidebar nav item for Recurring"
      contains: "repeat"
  key_links:
    - from: "src/pages/Recurring.jsx"
      to: "templates store"
      via: "addTemplate, deleteTemplate, fetchTemplates"
      pattern: "addTemplate|deleteTemplate|fetchTemplates"
    - from: "src/components/Layout.jsx"
      to: "/recurring"
      via: "NAV_ITEMS"
      pattern: "/recurring"
---

<objective>
Create recurring transactions management page and wire all new Phase 3 pages into the app router and sidebar navigation.

Purpose: Users can manage recurring transactions (CRUD on templates entity) and access all new Phase 3 features from the navigation.
Output: New Recurring.jsx page, updated App.jsx with new routes, updated Layout.jsx with new nav items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feature-gaps/03-CONTEXT.md
@src/pages/Recurring.jsx (to be created)
@src/App.jsx
@src/components/Layout.jsx
@src/lib/store/templates.js
@src/lib/store.js
@src/hooks/useData.js
@src/lib/financeUtils.ts
@src/lib/categories.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Recurring transactions page</name>
  <files>src/pages/Recurring.jsx</files>
  <read_first>
    - src/lib/store/templates.js (addTemplate, deleteTemplate, fetchTemplates, getTemplates — CRUD on templates table)
    - src/lib/store.js (barrel exports for templates)
    - src/pages/Budgets.jsx (page structure pattern — header, month nav, cards grid, modal)
    - src/pages/Goals.jsx (modal pattern with zod validation)
    - src/lib/financeUtils.ts (formatCurrency)
    - src/lib/categories.ts (DEFAULT_CATEGORIES, getCategories)
  </read_first>
  <action>
Create `src/pages/Recurring.jsx` per D-01/D-16:

1. Page structure (D-01: standalone page with sidebar nav):
   - Header: "Transações Recorrentes" title with "Gerencie suas transações fixas" subtitle
   - Container: `py-xl max-w-4xl mx-auto space-y-5`
   - "Nova Recorrência" button in header

2. Data fetching:
   - Call `fetchTemplates()` on mount to get templates from Supabase
   - Use `useEffect` + `useState` to manage templates list (same pattern as Settings.jsx rules)
   - Templates are stored in `templates` table with fields: id, description, value, category, payment_method, type

3. Templates list display:
   - Each template as a card: `bg-surface border border-surface-border p-card-padding`
   - Show: description, value (formatted), category, type (income/expense badge), payment method
   - Actions: edit, delete, toggle active/inactive
   - Grid: `grid grid-cols-1 sm:grid-cols-2 gap-3`
   - Empty state: "Nenhuma transação recorrente cadastrada" with "Criar primeira recorrência" button

4. Template CRUD modal (RecurringModal):
   - Fields: Descrição (text), Valor (number), Categoria (select from DEFAULT_CATEGORIES), Tipo (select: Receita/Despesa), Método de Pagamento (text or select), Dia do Mês (number 1-31)
   - Use `AdaptiveModal` from `@/components/ui/adaptive-modal`
   - Use `react-hook-form` + `zodResolver` for validation (same pattern as Goals.jsx)
   - Zod schema: description (min 2), value (positive number), category (min 1), type (enum income/expense)
   - On save: call `addTemplate(data)` or update existing template

5. Toggle active/inactive:
   - Add `active` field to template display (default true if not set)
   - Toggle switch on each card
   - When toggled, update template in Supabase (add `active` column to templates table if needed, or use a local overlay)

6. Delete with confirmation:
   - "Tem certeza que deseja excluir esta recorrência?" confirmation dialog
   - Call `deleteTemplate(id)` on confirm

7. Wire to existing cron function (D-16):
   - Show a note at the bottom: "As transações recorrentes são geradas automaticamente todo mês pela função generateRecurring."
   - No additional wiring needed — the cron function already reads from the templates table

8. Import patterns:
   - `import { fetchTemplates, addTemplate, deleteTemplate } from '@/lib/store'`
   - `import { formatCurrency } from '@/lib/financeUtils'`
   - `import { DEFAULT_CATEGORIES } from '@/lib/categories'`
   - `import MsIcon from '@/components/ui/ms-icon'`
   - `import { AdaptiveModal } from '@/components/ui/adaptive-modal'`
   - `import { Button } from '@/components/ui/button'`
   - `import { Input } from '@/components/ui/input'`
   - `import { Label } from '@/components/ui/label'`
   - `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'`
   - `import { Switch } from '@/components/ui/switch'`
  </action>
  <verify>
    <automated>grep -c "Transações Recorrentes\|fetchTemplates\|addTemplate\|deleteTemplate\|RecurringModal" src/pages/Recurring.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Recurring.jsx exists with "Transações Recorrentes" title
    - Page fetches and displays templates from the templates table
    - Each template shows: description, value, category, type, payment method
    - "Nova Recorrência" button opens a modal with form fields
    - Modal uses react-hook-form + zodResolver for validation
    - Edit and delete actions work on each template card
    - Toggle active/inactive switch exists on each card
    - Empty state shows when no templates exist
    - Page uses py-xl max-w-4xl mx-auto space-y-5 container
  </acceptance_criteria>
  <done>Recurring page shows CRUD interface for managing recurring transaction templates</done>
</task>

<task type="auto">
  <name>Task 2: Wire routes and navigation for all Phase 3 pages</name>
  <files>src/App.jsx, src/components/Layout.jsx</files>
  <read_first>
    - src/App.jsx (current route structure — 8 routes under ProtectedRoute)
    - src/components/Layout.jsx (NAV_ITEMS array with 6 items, BOTTOM_NAV with 4 items)
  </read_first>
<action>
Wire all new Phase 3 pages into the app router and sidebar navigation:

1. Update `src/App.jsx`:
   - Add imports at top:
     - `import Recurring from './pages/Recurring'`
     - `import Investments from './pages/Investments'`
   - Add routes inside the ProtectedRoute element (after existing routes, before closing tag):
     - `<Route path="/recurring" element={<ErrorBoundary><Recurring /></ErrorBoundary>} />`
     - `<Route path="/investments" element={<ErrorBoundary><Investments /></ErrorBoundary>} />`

2. Update `src/components/Layout.jsx` NAV_ITEMS array:
   - Add after the "Calendário" entry (index 3):
     - `{ path: '/investments', label: 'Investimentos', icon: 'account_balance_wallet', shortcut: '5' }`
     - `{ path: '/recurring', label: 'Recorrentes', icon: 'repeat', shortcut: '6' }`
   - Update existing shortcuts: "Relatórios" becomes shortcut '7', remove shortcut from "Configurações" (or make it '8')
   - Final NAV_ITEMS order: Dashboard(1), Transações(2), Planejamento(3), Calendário(4), Investimentos(5), Recorrentes(6), Relatórios(7), Configurações(8)

3. Update BOTTOM_NAV if needed:
   - Keep current 4 items (Home, Ledger, Stats, Config) — no change needed for mobile bottom nav

4. Update keyboard shortcut range:
   - Change `if (e.key >= '1' && e.key <= '7')` to `if (e.key >= '1' && e.key <= '8')` to accommodate the new nav items

5. Verify no import conflicts:
   - Ensure `Recurring` and `Investments` imports don't conflict with existing imports
   - Use named imports if needed: `import Recurring from './pages/Recurring'`
  </action>
  <verify>
    <automated>grep -c "/recurring\|/investments\|Recurring\|Investments" src/App.jsx src/components/Layout.jsx</automated>
  </verify>
  <acceptance_criteria>
    - App.jsx imports Recurring and Investments pages
    - App.jsx has routes for /recurring and /investments inside ProtectedRoute
    - Layout.jsx NAV_ITEMS includes "Investimentos" and "Recorrentes" entries
    - NAV_ITEMS has 8 entries total with correct icons and shortcuts
    - Keyboard shortcut range updated to support 8 nav items
    - Routes use ErrorBoundary wrapper (same pattern as existing routes)
  </acceptance_criteria>
  <done>All Phase 3 pages (Recurring, Investments) are wired into the app router and sidebar navigation</done>
</task>

</tasks>

<verification>
1. Recurring.jsx exists with full CRUD for templates
2. App.jsx has routes for /recurring and /investments
3. Layout.jsx has nav items for Investimentos and Recorrentes
4. All existing routes and nav items still work
</verification>

<success_criteria>
- User can access /recurring from sidebar and manage recurring transactions
- User can access /investments from sidebar
- Sidebar shows 8 nav items with correct icons and shortcuts
- All existing pages remain accessible
</success_criteria>

<output>
Create `.planning/phases/03-feature-gaps/03-05-SUMMARY.md` when done
</output>
