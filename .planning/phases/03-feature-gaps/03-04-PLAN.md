---
phase: 03-feature-gaps
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Settings.jsx
  - src/pages/Dashboard.jsx
  - src/components/settings/TabNotificacoes.jsx
autonomous: true
requirements:
  - "GAPS #5"
  - "GAPS #10"
  - "GAPS #11"

must_haves:
  truths:
    - "User can configure which notifications to receive"
    - "User can set custom budget alert thresholds"
    - "User can add manual bill reminders with due dates"
    - "Dashboard shows automated insights (top category change, savings rate)"
  artifacts:
    - path: "src/components/settings/TabNotificacoes.jsx"
      provides: "Notification preferences and bill reminders tab"
      contains: "TabNotificacoes"
    - path: "src/pages/Settings.jsx"
      provides: "Settings page with notification tab"
      contains: "notificacoes"
    - path: "src/pages/Dashboard.jsx"
      provides: "Dashboard with insights section"
      contains: "Insights"
  key_links:
    - from: "src/components/settings/TabNotificacoes.jsx"
      to: "notification store"
      via: "localStorage preferences"
      pattern: "notif_prefs"
    - from: "src/pages/Dashboard.jsx"
      to: "useBudgets, useTransactions"
      via: "insight calculations"
      pattern: "insights"
---

<objective>
Add notification preferences tab to Settings and automated insights section to Dashboard.

Purpose: Users can configure notification preferences, set budget alert thresholds, manage bill reminders, and see automated financial insights.
Output: New TabNotificacoes.jsx component, updated Settings.jsx with new tab, updated Dashboard.jsx with insights section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feature-gaps/03-CONTEXT.md
@src/pages/Settings.jsx
@src/pages/Dashboard.jsx
@src/components/settings/TabPersonalizacao.jsx
@src/components/settings/TabAutomacao.jsx
@src/lib/notifications.js
@src/lib/notificationStore.js
@src/lib/financeUtils.ts
@src/lib/store/settings.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create notification preferences and bill reminders tab</name>
  <files>src/components/settings/TabNotificacoes.jsx, src/pages/Settings.jsx</files>
  <read_first>
    - src/pages/Settings.jsx (current tab structure: personalizacao, automacao, backup)
    - src/components/settings/TabPersonalizacao.jsx (tab component pattern)
    - src/components/settings/TabAutomacao.jsx (tab component pattern)
    - src/lib/notifications.js (createNotification, generateBudgetAlerts)
    - src/lib/notificationStore.js (generateBudgetNotifications)
    - src/lib/store/settings.js (getSalaryConfig, saveSalaryConfig pattern)
  </read_first>
  <action>
Create TabNotificacoes.jsx and wire it into Settings.jsx per D-03/D-15:

1. Create `src/components/settings/TabNotificacoes.jsx`:

2. Budget alert preferences section (D-15):
   - Toggle: "Alertas de orçamento" (on/off) — stored in localStorage as `notif_prefs_budget_alerts`
   - Threshold slider/input: "Alertar quando atingir X% do orçamento" — default 80, range 50-100
   - Stored in localStorage as `notif_prefs_budget_threshold`
   - Use `Switch` component from `@/components/ui/switch`
   - Use `Input` type="number" with min=50, max=100, step=5 for threshold

3. Bill reminders section (GAPS #5):
   - List of manual bill reminders stored in localStorage as `notif_bill_reminders`
   - Each reminder: `{ id, name, dueDay, amount, active }`
   - CRUD: add new reminder, edit, toggle active/inactive, delete
   - "Adicionar lembrete" button opens a small inline form or modal:
     - Nome (text input)
     - Dia do vencimento (number input, 1-31)
     - Valor (number input, optional)
   - Display as a list with toggle switches and delete buttons
   - Use the same card pattern as TabAutomacao rules list

4. Notification delivery preferences:
   - Toggle: "Notificações no navegador" (on/off) — wraps existing `Notification.requestPermission()` logic
   - If browser notifications are denied, show a note: "Notificações bloqueadas pelo navegador"

5. Wire into Settings.jsx:
   - Add tab to TABS array: `{ id: 'notificacoes', label: 'Notificações', icon: 'notifications' }`
   - Add tab content: `{activeTab === 'notificacoes' && <TabNotificacoes />}`
   - Import TabNotificacoes at top of Settings.jsx

6. Create localStorage helpers for notification preferences:
   - `getNotifPrefs()` — returns `{ budgetAlerts: boolean, budgetThreshold: number, browserNotif: boolean }`
   - `saveNotifPrefs(prefs)` — saves to localStorage key `notif_prefs`
   - `getBillReminders()` — returns array of reminders
   - `saveBillReminders(reminders)` — saves to localStorage key `notif_bill_reminders`
   - Define these inside TabNotificacoes.jsx (local helpers, not exported)
  </action>
  <verify>
    <automated>grep -c "TabNotificacoes\|notif_prefs\|notif_bill_reminders" src/components/settings/TabNotificacoes.jsx src/pages/Settings.jsx</automated>
  </verify>
  <acceptance_criteria>
    - TabNotificacoes.jsx exists with budget alert toggle, threshold input, bill reminders list, and browser notification toggle
    - Settings.jsx has a "Notificações" tab in the TABS array with icon "notifications"
    - TabNotificacoes is imported and rendered when activeTab === 'notificacoes'
    - Budget alert threshold is configurable (50-100%, default 80%)
    - Bill reminders support add, edit, toggle active/inactive, delete
    - Preferences stored in localStorage keys: notif_prefs, notif_bill_reminders
  </acceptance_criteria>
  <done>Settings page has a "Notificações" tab with budget alert preferences, bill reminders, and browser notification toggle</done>
</task>

<task type="auto">
  <name>Task 2: Add automated insights section to Dashboard</name>
  <files>src/pages/Dashboard.jsx</files>
  <read_first>
    - src/pages/Dashboard.jsx (current implementation — health score, KPIs, charts, sections)
    - src/lib/financeUtils.ts (filterByMonth, calcTotals, groupByCategory, formatCurrency)
    - src/lib/categories.ts (MONTH_NAMES, CAT_COLORS)
  </read_first>
  <action>
Add an "Insights" section to Dashboard per D-06/D-13:

1. Create an `InsightsSection` component (inline in Dashboard.jsx) that generates automated financial insights.

2. Insight calculations (using existing data: transactions, budgets, monthTx, totals):
   - **Top category change**: Compare current month's top expense category with previous month. If spending in a category increased >30%, flag it. Show: "Seus gastos em {categoria} aumentaram {X}% em relação ao mês anterior."
   - **Savings rate**: Already calculated in healthData. Show: "Sua taxa de poupança é de {X}% este mês." with color coding (green >= 20%, amber >= 10%, red < 10%).
   - **Spending anomaly**: If any category's spending is >2x the average of the last 3 months, flag it. Show: "Gasto em {categoria} está {X}x acima da média dos últimos 3 meses."
   - **Budget adherence**: If all budgets are within limits, show positive insight. If any are over, show warning.
   - **Top merchant/description**: Find the most frequent transaction description this month. Show: "Sua transação mais frequente este mês é '{descricao}' ({X} vezes)."

3. Generate up to 3 insights (D-13: ">=3 automated insights"):
   - Priority: spending anomaly > category change > budget adherence > savings rate > top merchant
   - Each insight is a card with: icon, title (bold), description (text-sm), color indicator

4. Insight card styling:
   - Container: `bg-surface border border-surface-border p-card-padding`
   - Icon: 24px MsIcon, colored by insight type
   - Title: `font-title text-title`
   - Description: `font-body-sm text-body-sm text-muted-foreground`
   - Left border color by type: anomaly (red), change (amber), positive (green), neutral (blue)

5. Section placement: Add as a new DashSection after the existing sections, before the month navigation pill.
   - Section id: 'insights'
   - Title: "Insights Financeiros"
   - Icon: "auto_awesome"
   - Default open: true

6. Add 'insights' to the default dashSections configuration so it appears by default. Check `getDashSections()` in store/settings.js — if 'insights' is not in the default list, add it.

7. If no insights can be generated (insufficient data), show: "Continue registrando transações para receber insights personalizados."
  </action>
  <verify>
    <automated>grep -c "InsightsSection\|Insights Financeiros\|auto_awesome\|insights" src/pages/Dashboard.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.jsx contains an InsightsSection component
    - Section renders as a DashSection with id "insights" and title "Insights Financeiros"
    - At least 3 insight types are calculated: category change, savings rate, spending anomaly
    - Each insight displays as a card with icon, title, description, and colored left border
    - Insights use existing transaction and budget data (no new API calls)
    - Empty state shows message when insufficient data
    - Section is added to the default dashSections configuration
  </acceptance_criteria>
  <done>Dashboard shows an "Insights Financeiros" section with at least 3 automated financial insights</done>
</task>

</tasks>

<verification>
1. Settings has a "Notificações" tab with budget alert preferences and bill reminders
2. Dashboard shows "Insights Financeiros" section with automated insights
3. Notification preferences are stored in localStorage
</verification>

<success_criteria>
- User can toggle budget alerts and set custom threshold (50-100%)
- User can add, edit, toggle, and delete bill reminders
- Dashboard shows >=3 automated insights (category change, savings rate, anomaly)
- Insights use real transaction and budget data
</success_criteria>

<output>
Create `.planning/phases/03-feature-gaps/03-04-SUMMARY.md` when done
</output>
