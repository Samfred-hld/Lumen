---
phase: 03-feature-gaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Planejamento.jsx
autonomous: true
requirements:
  - "GAPS #4"

must_haves:
  truths:
    - "User can see all budget categories side-by-side with actual spending"
    - "Budget comparison shows % used per category"
    - "Month-over-month sparklines show spending trends per category"
  artifacts:
    - path: "src/pages/Planejamento.jsx"
      provides: "Budget comparison tab with sparklines"
      contains: "Comparação"
  key_links:
    - from: "src/pages/Planejamento.jsx"
      to: "useBudgets, useTransactions"
      via: "React Query hooks"
      pattern: "useBudgets|useTransactions"
---

<objective>
Add a "Comparacao" tab to the Planejamento page showing budget vs actual comparison with month-over-month sparklines.

Purpose: Users can compare budgeted vs actual spending across categories and see trends over time.
Output: Updated Planejamento.jsx with a comparison tab, table view, and sparkline charts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feature-gaps/03-CONTEXT.md
@src/pages/Planejamento.jsx
@src/pages/Budgets.jsx
@src/lib/financeUtils.ts
@src/lib/categories.ts
@src/hooks/useData.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add comparison tab with budgeted vs actual table</name>
  <files>src/pages/Planejamento.jsx</files>
  <read_first>
    - src/pages/Planejamento.jsx (current implementation with budget cards and goals)
    - src/pages/Budgets.jsx (budget card patterns, summary cards)
    - src/lib/financeUtils.ts (filterByMonth, calcTotals, getMonthKey, formatCurrency)
    - src/lib/categories.ts (DEFAULT_CATEGORIES, CAT_COLORS)
  </read_first>
  <action>
Add a tabbed view to Planejamento with "Orcamentos" (existing) and "Comparacao" (new) tabs per D-02/D-11:

1. Add tab state: `const [activeTab, setActiveTab] = useState('orcamentos')`. Two tabs: "Orcamentos" (existing content) and "Comparacao" (new comparison view).

2. Tab UI: Use the same tab pattern as Settings.jsx — a `flex gap-1 bg-muted/50 p-1 rounded` container with tab buttons. Tab labels: "Orçamentos" and "Comparação". Each tab button uses `flex-1 flex items-center justify-center gap-1.5 py-2 px-3 rounded text-sm font-medium transition-all` with active state `bg-card text-foreground shadow-sm` and inactive `text-muted-foreground hover:text-foreground`.

3. Wrap existing Planejamento content (budget cards, goals, budget manager) inside `{activeTab === 'orcamentos' && (...)}`.

4. Create comparison view for `{activeTab === 'comparacao' && (...)}`:

5. Comparison table (D-11: budgeted vs actual, % used):
   - Table with columns: Categoria, Orcamento, Gasto, % Usado, Status
   - Each row is a category that has a budget in the current month
   - "Orçamento" column: `b.limit` formatted with `formatCurrency`
   - "Gasto" column: actual spending calculated as `monthTx.filter(t => t.type === 'expense' && t.category === b.category).reduce((s, t) => s + t.value, 0)`
   - "% Usado" column: `(spent / limit * 100).toFixed(0)%`
   - "Status" column: green checkmark if <= 80%, amber warning if 80-100%, red alert if > 100%
   - Use `bg-surface border border-surface-border` for the table container
   - Table rows use `border-b border-surface-border` dividers
   - Category name shows with its CAT_COLORS dot

6. Summary row at bottom: total budgeted, total spent, overall % used.

7. If no budgets exist for the month, show empty state: "Nenhum orçamento para comparar. Crie orçamentos na aba 'Orçamentos'."

8. Month navigation: reuse the existing month navigation from the "Orcamentos" tab (the component already has `useMonthNavigation`).
  </action>
  <verify>
    <automated>grep -c "Comparação\|activeTab\|comparacao" src/pages/Planejamento.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Planejamento.jsx has a tabbed interface with "Orçamentos" and "Comparação" tabs
    - Comparison tab shows a table with columns: Categoria, Orçamento, Gasto, % Usado, Status
    - Each budget category appears as a row with budgeted amount, actual spending, and percentage
    - Status column shows green (<=80%), amber (80-100%), red (>100%) indicators
    - Summary row shows total budgeted, total spent, overall percentage
    - Empty state shows message when no budgets exist for the month
    - Tab styling matches Settings.jsx tab pattern
  </acceptance_criteria>
  <done>Planejamento has a "Comparação" tab showing budget vs actual comparison table with status indicators</done>
</task>

<task type="auto">
  <name>Task 2: Add month-over-month sparklines per category</name>
  <files>src/pages/Planejamento.jsx</files>
  <read_first>
    - src/pages/Planejamento.jsx (after Task 1 — comparison tab exists)
    - src/lib/financeUtils.ts (filterByMonth, getMonthKey)
    - src/lib/categories.ts (MONTH_NAMES)
  </read_first>
  <action>
Add sparkline charts to the comparison tab showing spending trends per category:

1. For each budget category in the comparison table, add a sparkline column showing the last 6 months of spending.

2. Sparkline data calculation:
   - For each category, calculate spending for the last 6 months (including current month)
   - `for (let i = 5; i >= 0; i--) { let m = currentMonth - i, y = currentYear; if (m < 0) { m += 12; y--; } ... }`
   - Filter transactions by month and category, sum values
   - Result: array of 6 numbers representing monthly spending

3. Inline SVG sparkline component (create as a local function `Sparkline` inside Planejamento.jsx):
   - Width: 80px, Height: 24px
   - Polyline with `stroke` color matching the category's `CAT_COLORS[color]` or default `#10b981`
   - `stroke-width="1.5"`, `fill="none"`, `stroke-linecap="round"`, `stroke-linejoin="round"`
   - Scale values to fit within the SVG viewBox
   - No axes, no labels — just the line

4. Add "Tendencia" column to the comparison table between "% Usado" and "Status".

5. If a category has no spending in any of the 6 months, show a dash "-" instead of a sparkline.

6. Add a mini legend below the table: "Tendência: últimos 6 meses" in `text-xs text-muted-foreground`.
  </action>
  <verify>
    <automated>grep -c "Sparkline\|Tendência\|polyline" src/pages/Planejamento.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Comparison table has a "Tendência" column with inline SVG sparklines
    - Each sparkline shows 6 months of spending data for that category
    - Sparkline color matches the category's CAT_COLORS value
    - Categories with no spending show "-" instead of a sparkline
    - Legend text "Tendência: últimos 6 meses" appears below the table
    - Sparklines are 80px wide, 24px tall, using polyline SVG
  </acceptance_criteria>
  <done>Comparison tab shows month-over-month sparklines per budget category</done>
</task>

</tasks>

<verification>
1. Planejamento has "Orçamentos" and "Comparação" tabs
2. Comparison tab shows budgeted vs actual with % used and status
3. Sparklines show 6-month trends per category
</verification>

<success_criteria>
- User can switch between "Orçamentos" and "Comparação" tabs
- Comparison table shows all budget categories with budgeted, actual, % used, status
- Sparklines visualize 6-month spending trends per category
- Status indicators use green/amber/red colors correctly
</success_criteria>

<output>
Create `.planning/phases/03-feature-gaps/03-02-SUMMARY.md` when done
</output>
