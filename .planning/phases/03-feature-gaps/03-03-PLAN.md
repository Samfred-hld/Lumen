---
phase: 03-feature-gaps
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Investments.jsx
  - src/lib/insights.ts
autonomous: true
requirements:
  - "GAPS #1"

must_haves:
  truths:
    - "User can view investment goals in a portfolio view"
    - "User can see allocation by investment type (pie chart)"
    - "User can see total invested, total target, and basic ROI"
    - "User can add manual investment entries"
  artifacts:
    - path: "src/pages/Investments.jsx"
      provides: "Investment portfolio page"
      contains: "Investments"
    - path: "src/lib/insights.ts"
      provides: "Investment calculation utilities"
      exports: ["calculateInvestmentStats"]
  key_links:
    - from: "src/pages/Investments.jsx"
      to: "Goal entity with investmentType field"
      via: "useGoals hook"
      pattern: "investmentType"
---

<objective>
Create an investment tracking page that uses the existing Goal entity's investmentType field to show a portfolio view with allocation, totals, and ROI.

Purpose: Users can track their investments (manual entry) with allocation visualization and basic performance metrics.
Output: New Investments.jsx page, new insights.ts utility file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feature-gaps/03-CONTEXT.md
@src/pages/Goals.jsx
@src/hooks/useData.js
@src/lib/financeUtils.ts
@src/components/dashboard/KpiCard.jsx
@src/components/ui/ms-icon.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create investment calculation utilities</name>
  <files>src/lib/insights.ts</files>
  <read_first>
    - src/lib/financeUtils.ts (formatCurrency, getGoalProgress patterns)
    - src/pages/Goals.jsx (Goal entity structure: name, targetValue, currentValue, progressMode, deadline, color, description, investmentType)
  </read_first>
  <action>
Create `src/lib/insights.ts` with investment calculation utilities:

1. Export `INVESTMENT_TYPES` constant:
   ```
   export const INVESTMENT_TYPES = [
     { value: 'renda_fixa', label: 'Renda Fixa', color: '#10b981' },
     { value: 'renda_variavel', label: 'Renda Variável', color: '#3b82f6' },
     { value: 'cripto', label: 'Criptomoedas', color: '#f59e0b' },
     { value: 'fundos', label: 'Fundos', color: '#8b5cf6' },
     { value: 'imoveis', label: 'Imóveis', color: '#ec4899' },
     { value: 'outros', label: 'Outros', color: '#6b7280' },
   ];
   ```

2. Export `calculateInvestmentStats(goals, transactions)` function:
   - Filter goals that have `investmentType` set (truthy)
   - For each investment goal, calculate:
     - `invested`: `getGoalProgress(goal, transactions)` (uses existing function)
     - `target`: `goal.targetValue`
     - `progress`: `invested / target * 100` (capped at 100)
   - Return object:
     ```
     {
       totalInvested: number,
       totalTarget: number,
       overallProgress: number, // percentage
       byType: Array<{
         type: string,
         label: string,
         color: string,
         invested: number,
         target: number,
         count: number,
         percentage: number, // allocation % of total invested
       }>,
       goals: Array<{
         id: string,
         name: string,
         type: string,
         typeLabel: string,
         typeColor: string,
         invested: number,
         target: number,
         progress: number,
         deadline: string | null,
       }>
     }
     ```

3. Export `getInvestmentTypeLabel(typeValue)` helper:
   - Returns the label from INVESTMENT_TYPES for a given value
   - Falls back to "Outros" if not found

4. Export `getInvestmentTypeColor(typeValue)` helper:
   - Returns the color from INVESTMENT_TYPES for a given value
   - Falls back to "#6b7280" if not found
  </action>
  <verify>
    <automated>grep -c "calculateInvestmentStats\|INVESTMENT_TYPES\|getInvestmentTypeLabel" src/lib/insights.ts</automated>
  </verify>
  <acceptance_criteria>
    - insights.ts exports INVESTMENT_TYPES array with 6 types (renda_fixa, renda_variavel, cripto, fundos, imoveis, outros)
    - Each type has value, label, and color properties
    - calculateInvestmentStats takes goals and transactions, returns stats object
    - Stats object contains totalInvested, totalTarget, overallProgress, byType array, goals array
    - byType entries include percentage (allocation % of total invested)
    - getInvestmentTypeLabel and getInvestmentTypeColor are exported
  </acceptance_criteria>
  <done>insights.ts provides investment calculation utilities with type definitions and stats computation</done>
</task>

<task type="auto">
  <name>Task 2: Create Investments page with portfolio view</name>
  <files>src/pages/Investments.jsx</files>
  <read_first>
    - src/lib/insights.ts (created in Task 1 — INVESTMENT_TYPES, calculateInvestmentStats)
    - src/pages/Goals.jsx (Goal entity patterns, GoalModal, DepositModal)
    - src/components/dashboard/KpiCard.jsx (KPI card component for stats)
    - src/lib/financeUtils.ts (formatCurrency, formatDate)
    - src/hooks/useData.js (useGoals, useTransactions)
  </read_first>
  <action>
Create `src/pages/Investments.jsx` per D-04/D-14:

1. Page structure (D-04: new page `src/pages/Investments.jsx`):
   - Header: "Investimentos" title with "Acompanhe sua carteira de investimentos" subtitle
   - Use `py-xl max-w-5xl mx-auto space-y-5` container (same pattern as Goals.jsx)
   - Add button: "Novo Investimento" (opens GoalModal with investmentType pre-filled)

2. KPI summary section at top:
   - Use KpiCard component (import from `@/components/dashboard/KpiCard`)
   - 3 KPI cards in a grid:
     - "TOTAL INVESTIDO" — `formatCurrency(stats.totalInvested)` with `icon="account_balance_wallet" variant="investment"`
     - "META TOTAL" — `formatCurrency(stats.totalTarget)` with `icon="flag" variant="balance"`
     - "PROGRESSO" — `${stats.overallProgress}%` with `icon="trending_up" variant="income"`
   - Grid: `grid grid-cols-1 sm:grid-cols-3 gap-sm md:gap-md`

3. Allocation pie chart (D-11 equivalent for investments):
   - Simple SVG pie chart showing allocation by investment type
   - Each slice colored by `type.color`
   - Legend below with type labels and percentages
   - Pie chart: 200x200px SVG using `<circle>` with `stroke-dasharray` technique (conic gradient alternative)
   - If only one type, show a full circle
   - If no investments, show empty state: "Nenhum investimento registrado"

4. Investment goals list:
   - Grid of goal cards (reuse GoalCard component from `@/components/finance/GoalCard`)
   - Each card shows: name, invested/target, progress bar, investment type badge
   - Filter to only show goals with `investmentType` set
   - Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`

5. Wire up GoalModal for adding/editing investments:
   - Import `GoalModal, DepositModal` from `@/pages/PlanejamentoModals`
   - When "Novo Investimento" is clicked, open GoalModal
   - After save, refetch goals
   - Include `investmentType` field in the modal — add a select for investment type INVESTMENT_TYPES

6. Add investment type select to GoalModal:
   - Import `Select, SelectContent, SelectItem, SelectTrigger, SelectValue` from `@/components/ui/select`
   - Add a "Tipo de Investimento" field in the modal form
   - Options from INVESTMENT_TYPES
   - This modifies PlanejamentoModals.jsx — add the select field to GoalModal

7. Empty state when no investment goals exist:
   - Icon: `account_balance_wallet` (32px, muted)
   - Text: "Nenhum investimento registrado"
   - Button: "Criar primeiro investimento"
  </action>
  <verify>
    <automated>grep -c "Investimentos\|calculateInvestmentStats\|investmentType\|INVESTMENT_TYPES" src/pages/Investments.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Investments.jsx exists with "Investimentos" title and subtitle
    - 3 KPI cards show: total invested, target total, overall progress %
    - SVG pie chart shows allocation by investment type with legend
    - Investment goals displayed in card grid using GoalCard component
    - "Novo Investimento" button opens GoalModal
    - Empty state shows when no investment goals exist
    - Page uses py-xl max-w-5xl mx-auto space-y-5 container pattern
  </acceptance_criteria>
  <done>Investments page shows portfolio view with KPIs, allocation pie chart, and investment goal cards</done>
</task>

</tasks>

<verification>
1. insights.ts exports INVESTMENT_TYPES and calculateInvestmentStats
2. Investments.jsx renders KPI cards, pie chart, and goal cards
3. Goals with investmentType field appear in the investments view
</verification>

<success_criteria>
- User can view investment portfolio on /investments page
- Allocation pie chart shows distribution by investment type
- KPI cards show total invested, target, and progress
- User can add new investment goals via the modal
</success_criteria>

<output>
Create `.planning/phases/03-feature-gaps/03-03-SUMMARY.md` when done
</output>
