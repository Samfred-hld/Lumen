---
phase: 02-ui-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/iconMap.js
  - src/components/GlobalSearch.jsx
  - src/components/dashboard/DashSection.jsx
  - src/components/dashboard/OnboardingModal.jsx
  - src/components/finance/CSVImport.jsx
  - src/components/finance/CashFlowForecast.jsx
  - src/components/finance/ColumnMapper.jsx
  - src/components/finance/DashCustomizeModal.jsx
  - src/components/finance/InstallmentConfirm.jsx
  - src/components/finance/QuickEntry.jsx
  - src/components/finance/SuggestionBanner.jsx
  - src/components/finance/TransactionInlineForm.jsx
  - src/components/finance/TransactionRow.jsx
  - src/components/settings/TabAutomacao.jsx
  - src/components/settings/TabDados.jsx
  - src/components/settings/TabPersonalizacao.jsx
  - src/pages/Budgets.jsx
  - src/pages/CalendarPage.jsx
  - src/pages/Goals.jsx
  - src/pages/Planejamento.jsx
  - src/pages/Reports.jsx
  - src/pages/Settings.jsx
  - src/pages/Transactions.jsx
autonomous: true
requirements:
  - CONCERN #11
  - UI-REVIEW #1-22

must_haves:
  truths:
    - "Zero lucide-react imports remain in custom code (non-shadcn files)"
    - "All icons render correctly using Material Symbols via MsIcon"
    - "iconMap.js exists as single source of truth for icon mappings"
  artifacts:
    - path: "src/lib/iconMap.js"
      provides: "Centralized Lucide to Material Symbols mapping"
      exports: ["iconMap", "LUCIDE_TO_MATERIAL"]
    - path: "src/components/ui/ms-icon.jsx"
      provides: "MsIcon wrapper component (already exists, no changes)"
      exports: ["default"]
  key_links:
    - from: "src/lib/iconMap.js"
      to: "src/components/ui/ms-icon.jsx"
      via: "icon name strings passed to MsIcon"
      pattern: "MsIcon.*name="
---

<objective>
Migrate all 22 custom component files from lucide-react to Material Symbols icons via the existing MsIcon wrapper component. Create a centralized iconMap.js mapping file as the single source of truth for all icon replacements.

Purpose: Eliminate mixed icon library usage (CONCERN #11). Material Symbols is already loaded and MsIcon wrapper exists — this migration unifies all custom code to one icon system.

Output: src/lib/iconMap.js (new), all 22 custom files updated to use MsIcon instead of lucide-react imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-consolidation/02-CONTEXT.md
@.planning/phases/02-ui-consolidation/02-RESEARCH.md
@.planning/phases/02-ui-consolidation/02-UI-SPEC.md

<interfaces>
<!-- MsIcon component API — already exists, import from this path -->
From src/components/ui/ms-icon.jsx:
```jsx
export default function MsIcon({ name, className, size = 24, filled = false }) {
  return (
    <span
      className={cn('material-symbols-outlined', className)}
      style={{ fontSize: size, fontVariationSettings: `'FILL' ${filled ? 1 : 0}` }}
      aria-hidden="true"
    >
      {name}
    </span>
  );
}
```

<!-- Existing local MsIcon definitions that MUST be removed (anti-pattern) -->
From src/components/finance/TransactionRow.jsx (lines 13-15):
```jsx
function MsIcon({ name, className, size = 20 }) {
  return <span className={cn('material-symbols-outlined', className)} style={{ fontSize: size }}>{name}</span>;
}
```

From src/pages/Planejamento.jsx (lines 23-31):
```jsx
function MsIcon({ name, className, size = 24, filled = false }) {
  return (
    <span className={cn('material-symbols-outlined', className)}
      style={{ fontSize: size, fontVariationSettings: `'FILL' ${filled ? 1 : 0}` }}>
      {name}
    </span>
  );
}
```

From src/pages/Dashboard.jsx (lines 22-28):
```jsx
function MsIcon({ name, className, size = 24 }) {
  return (
    <span className={cn('material-symbols-outlined', className)} style={{ fontSize: size }}>
      {name}
    </span>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create centralized iconMap.js</name>
  <files>src/lib/iconMap.js</files>
  <read_first>
    src/components/ui/ms-icon.jsx
    src/pages/Planejamento.jsx (lines 35-70 for CAT_MATERIAL_ICONS)
    src/components/finance/TransactionRow.jsx (lines 18-29 for getCategoryMsIcon)
    02-UI-SPEC.md (Icon Migration Contract section)
  </read_first>
  <action>
Create src/lib/iconMap.js with two exports:

1. `LUCIDE_TO_MATERIAL` — object mapping every Lucide icon name used in the 22 custom files to its Material Symbols equivalent. Based on UI-SPEC.md Icon Migration Contract:
   - Plus → add
   - Trash2 → delete
   - Pencil → edit
   - TrendingUp → trending_up
   - TrendingDown → trending_down
   - ChevronLeft → chevron_left
   - ChevronRight → chevron_right
   - Search → search
   - X → close
   - Download → download
   - Upload → upload
   - Copy → content_copy
   - Check → check
   - AlertTriangle → warning
   - FileText → description
   - CreditCard → credit_card
   - Plus (circle variant) → add_circle
   - Target → flag
   - CheckCircle2 → check_circle
   - Clock → schedule
   - History → history
   - Repeat → repeat
   - Moon → dark_mode
   - Sun → light_mode
   - AlertCircle → error
   - Receipt → receipt
   - Sparkles → auto_awesome
   - ArrowRight → arrow_forward
   - FileSpreadsheet → table_chart
   - Calendar → calendar_month
   - Settings → settings (if used)

2. `iconMap` — same object (alias for convenience)

Also export the `CAT_MATERIAL_ICONS` category mapping (from Planejamento.jsx lines 35-70) so it can be imported by BudgetCard, GoalCard, and TransactionRow instead of being duplicated.
  </action>
  <verify>
    <automated>node -e "const m = require('./src/lib/iconMap.js'); console.log(Object.keys(m.LUCIDE_TO_MATERIAL).length, 'mappings'); console.log(Object.keys(m.CAT_MATERIAL_ICONS).length, 'category icons')"</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/iconMap.js exists and exports LUCIDE_TO_MATERIAL, iconMap, CAT_MATERIAL_ICONS
    - LUCIDE_TO_MATERIAL contains at least 25 key-value pairs
    - CAT_MATERIAL_ICONS contains at least 20 category-to-icon mappings
    - All Material Symbols icon names use snake_case (e.g., trending_up, not TrendingUp)
    - File imports cn from @/lib/utils (for consistency with codebase patterns)
  </acceptance_criteria>
  <done>iconMap.js created with all Lucide-to-Material-Symbols mappings and category icon mappings exported</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all 22 custom files from lucide-react to MsIcon</name>
  <files>
    src/components/GlobalSearch.jsx
    src/components/dashboard/DashSection.jsx
    src/components/dashboard/OnboardingModal.jsx
    src/components/finance/CSVImport.jsx
    src/components/finance/CashFlowForecast.jsx
    src/components/finance/ColumnMapper.jsx
    src/components/finance/DashCustomizeModal.jsx
    src/components/finance/InstallmentConfirm.jsx
    src/components/finance/QuickEntry.jsx
    src/components/finance/SuggestionBanner.jsx
    src/components/finance/TransactionInlineForm.jsx
    src/components/finance/TransactionRow.jsx
    src/components/settings/TabAutomacao.jsx
    src/components/settings/TabDados.jsx
    src/components/settings/TabPersonalizacao.jsx
    src/pages/Budgets.jsx
    src/pages/CalendarPage.jsx
    src/pages/Goals.jsx
    src/pages/Planejamento.jsx
    src/pages/Reports.jsx
    src/pages/Settings.jsx
    src/pages/Transactions.jsx
  </files>
  <read_first>
    src/lib/iconMap.js (created in Task 1)
    src/components/ui/ms-icon.jsx
    02-UI-SPEC.md (Icon Migration Contract section)
    02-RESEARCH.md (Pattern 1: Icon Migration Pattern)
  </read_first>
  <action>
For each of the 22 custom files:

1. Remove the lucide-react import line (e.g., `import { Pencil, Trash2, Copy } from 'lucide-react';`)
2. Add `import MsIcon from '@/components/ui/ms-icon';` if not already present
3. Replace every Lucide icon JSX with MsIcon equivalent:
   - `<Pencil size={14} />` → `<MsIcon name="edit" size={14} />`
   - `<Trash2 size={14} />` → `<MsIcon name="delete" size={14} />`
   - `<Copy size={14} />` → `<MsIcon name="content_copy" size={14} />`
   - `<Plus size={24} />` → `<MsIcon name="add" size={24} />`
   - `<X size={24} />` → `<MsIcon name="close" size={24} />`
   - etc. (use LUCIDE_TO_MATERIAL from iconMap.js as reference)
4. For files that define a LOCAL MsIcon function (Dashboard.jsx, TransactionRow.jsx, Planejamento.jsx): DELETE the local definition and import from @/components/ui/ms-icon instead
5. For files that have getCategoryMsIcon or CAT_MATERIAL_ICONS: import from @/lib/iconMap.js instead of defining locally

DO NOT touch files in src/components/ui/ that are shadcn vendor code (fab.jsx, pagination.jsx, swipe-to-delete.jsx) — per D-08/D-13.

Special handling:
- TransactionRow.jsx: action buttons (Copy, Pencil, Trash2 at lines 91-99) → MsIcon with same size
- Planejamento.jsx: budget card action buttons (Pencil, Trash2 at lines 489-496) → MsIcon
- Planejamento.jsx: goal card CheckCircle2, AlertTriangle, Plus → MsIcon
- Dashboard.jsx: remove local MsIcon definition (lines 22-28), import canonical one
- FAB (fab.jsx): SKIP — this file is handled in Plan 02
  </action>
  <verify>
    <automated>grep -r "from 'lucide-react'" src/ --include='*.jsx' --include='*.js' | grep -v 'src/components/ui/' | grep -v 'node_modules' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep command above returns 0 (zero lucide-react imports in custom code)
    - All 22 files import MsIcon from @/components/ui/ms-icon (not local definitions)
    - No file contains `function MsIcon` or `const MsIcon` local definitions (except src/components/ui/ms-icon.jsx)
    - Dashboard.jsx, TransactionRow.jsx, Planejamento.jsx have no local MsIcon definitions
    - All icon JSX uses MsIcon component with name and size props
    - shadcn vendor files (fab.jsx, pagination.jsx, swipe-to-delete.jsx) are untouched
    - `npm test` passes (existing tests still work)
  </acceptance_criteria>
  <done>All 22 custom files migrated from lucide-react to MsIcon. Zero lucide-react imports in custom code. All local MsIcon definitions removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iconMap.js → MsIcon | Icon name strings must be valid Material Symbols names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-01 | Tampering | npm/pip/cargo installs | mitigate | No new packages installed — pure refactoring |
</threat_model>

<verification>
1. `grep -r "from 'lucide-react'" src/ --include='*.jsx' --include='*.js' | grep -v 'src/components/ui/' | grep -v 'node_modules'` returns empty
2. `grep -rn "function MsIcon\|const MsIcon" src/ --include='*.jsx' | grep -v 'src/components/ui/ms-icon.jsx'` returns empty
3. `npm test` passes
4. `npm run build` succeeds
</verification>

<success_criteria>
- Zero lucide-react imports in custom code (22 files migrated)
- Zero local MsIcon definitions outside src/components/ui/ms-icon.jsx
- iconMap.js exists with complete mappings
- All icons render correctly (visual verification in later plans)
</success_criteria>

<output>
Create `.planning/phases/02-ui-consolidation/02-01-SUMMARY.md` when done
</output>
