---
phase: 02-ui-consolidation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/ui/fab.jsx
  - src/components/finance/TransactionRow.jsx
autonomous: true
requirements:
  - CONCERN #6
  - CONCERN #7
  - CONCERN #8
  - UI-REVIEW #1-22

must_haves:
  truths:
    - "Exactly one FAB component exists in the codebase"
    - "FAB uses semantic color tokens, not hardcoded Tailwind colors"
    - "FAB is positioned at bottom-24 right-6 with md:hidden visibility"
    - "TransactionRow has single responsive render path"
    - "Mobile: swipe-to-delete + tap for edit. Desktop: visible action buttons on hover"
  artifacts:
    - path: "src/components/ui/fab.jsx"
      provides: "Single canonical FAB component"
      exports: ["default"]
    - path: "src/components/finance/TransactionRow.jsx"
      provides: "Responsive transaction row with MsIcon action buttons"
      exports: ["default"]
  key_links:
    - from: "src/components/ui/fab.jsx"
      to: "src/components/ui/ms-icon.jsx"
      via: "MsIcon import for icon rendering"
      pattern: "import MsIcon"
    - from: "src/components/finance/TransactionRow.jsx"
      to: "src/components/ui/ms-icon.jsx"
      via: "MsIcon import for action buttons"
      pattern: "import MsIcon"
---

<objective>
Consolidate the FAB component (fix hardcoded colors, standardize positioning/visibility) and refactor TransactionRow to a single responsive path with Material Symbols action buttons.

Purpose: Eliminate FAB fragmentation (CONCERN #6), hardcoded colors (CONCERN #8), and TransactionRow dual render paths (CONCERN #7). These are the two shared UI components that need cleanup before page-level changes.

Output: Updated fab.jsx and TransactionRow.jsx with design token compliance and responsive behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-consolidation/02-CONTEXT.md
@.planning/phases/02-ui-consolidation/02-RESEARCH.md
@.planning/phases/02-ui-consolidation/02-UI-SPEC.md

<interfaces>
<!-- Current FAB component — needs color + position + icon fixes -->
From src/components/ui/fab.jsx:
- Props: { onNewIncome, onNewExpense, onNewTransaction }
- Current position: fixed bottom-20 right-4 z-50
- Current colors: bg-emerald-500 (income), bg-red-500 (expense)
- Current icons: Plus, TrendingUp, TrendingDown, X from lucide-react
- Visibility: md:hidden (already correct)

<!-- Current TransactionRow — needs icon migration + responsive fix -->
From src/components/finance/TransactionRow.jsx:
- Props: { t, index, onDelete, onEdit, onDuplicate, isSelected, onSelect }
- Already uses 12-col grid layout
- Action buttons at col-span-1 hidden lg:flex (lines 90-100)
- Uses lucide-react: Pencil, Trash2, Copy (to be migrated to MsIcon in Plan 01)
- SwipeToDelete wrapper handles mobile swipe

<!-- Design tokens from tailwind.config.js -->
From tailwind.config.js:
- bg-primary: teal accent (#005c55 / #0F766E)
- bg-success: green (#10B981)
- bg-danger: red (#DC2626)
- text-primary-foreground: white on primary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate FAB component</name>
  <files>src/components/ui/fab.jsx</files>
  <read_first>
    src/components/ui/fab.jsx
    src/components/ui/ms-icon.jsx
    tailwind.config.js (color tokens section)
    02-UI-SPEC.md (FAB Visibility section, Color section)
    02-CONTEXT.md (D-06 standardization)
  </read_first>
  <action>
Refactor src/components/ui/fab.jsx:

1. Replace lucide-react imports with MsIcon:
   - `import { Plus, TrendingUp, TrendingDown, X } from 'lucide-react'` → remove
   - Add `import MsIcon from '@/components/ui/ms-icon'`

2. Replace icon JSX:
   - `<Plus size={24} />` → `<MsIcon name="add" size={24} />`
   - `<X size={24} />` → `<MsIcon name="close" size={24} />`
   - `<TrendingUp size={20} />` → `<MsIcon name="trending_up" size={20} />`
   - `<TrendingDown size={20} />` → `<MsIcon name="trending_down" size={20} />`

3. Fix hardcoded colors (CONCERN #8):
   - `bg-emerald-500` → `bg-success` (income sub-button)
   - `bg-emerald-600` → `bg-success/90` (hover state)
   - `bg-red-500` → `bg-danger` (expense sub-button)
   - `bg-red-600` → `bg-danger/90` (hover state)

4. Standardize positioning (per UI-SPEC.md):
   - `fixed bottom-20 right-4` → `fixed bottom-24 right-6`
   - z-50 is already correct

5. Keep md:hidden visibility (already correct)

6. Ensure aria-labels remain: "Nova receita", "Nova despesa", "Nova transação", "Fechar"
  </action>
  <verify>
    <automated>grep -c "lucide-react\|bg-emerald\|bg-red-" src/components/ui/fab.jsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains zero lucide-react imports
    - File contains zero hardcoded color classes (bg-emerald-*, bg-red-*)
    - FAB positioned at bottom-24 right-6 (grep for "bottom-24 right-6")
    - FAB visibility is md:hidden (grep for "md:hidden")
    - All icons use MsIcon component with Material Symbols names
    - Sub-button colors use semantic tokens: bg-success, bg-danger
    - aria-labels preserved in Portuguese
  </acceptance_criteria>
  <done>FAB consolidated: single component, semantic colors, correct positioning, Material Symbols icons</done>
</task>

<task type="auto">
  <name>Task 2: Refactor TransactionRow responsive behavior</name>
  <files>src/components/finance/TransactionRow.jsx</files>
  <read_first>
    src/components/finance/TransactionRow.jsx
    src/components/ui/ms-icon.jsx
    src/components/ui/swipe-to-delete.jsx
    02-CONTEXT.md (D-01, D-02, D-04)
    02-UI-SPEC.md (Interaction Contracts: TransactionRow Responsive Behavior)
    02-RESEARCH.md (Pattern 4: Responsive Action Buttons Pattern)
  </read_first>
  <action>
Refactor src/components/finance/TransactionRow.jsx per D-01, D-02, D-04:

1. Ensure MsIcon is imported from @/components/ui/ms-icon (Plan 01 should have done this, verify)
2. Ensure action buttons use MsIcon (Plan 01 should have done this, verify):
   - Copy → content_copy
   - Pencil → edit
   - Trash2 → delete

3. Verify responsive layout per D-01/D-02/D-04:
   - Mobile (< md): SwipeToDelete wrapper handles swipe-to-delete, tap opens edit
   - Desktop (>= md): 12-col grid with visible action buttons on hover
   - Action buttons: `hidden lg:flex` (already correct at line 90)
   - The current implementation already has single responsive path with 12-col grid

4. Ensure action buttons have proper touch targets (44px minimum per WCAG):
   - Current: `className="p-1 hover:..."` — p-1 is 4px, button content is 14px icon = 22px total
   - Fix: `className="p-2 hover:..."` — p-2 is 8px, button content is 14px icon = 30px total
   - Or use min-w-[44px] min-h-[44px] for explicit compliance

5. Verify the local MsIcon definition was removed by Plan 01 (if not, remove it and import from @/components/ui/ms-icon)

Note: The current TransactionRow already has a single responsive render path (not dual). The main work is ensuring icon migration is complete and touch targets meet WCAG requirements.
  </action>
  <verify>
    <automated>grep -c "lucide-react\|function MsIcon\|const MsIcon" src/components/finance/TransactionRow.jsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains zero lucide-react imports
    - File contains zero local MsIcon definitions
    - MsIcon imported from @/components/ui/ms-icon
    - Action buttons use MsIcon with Material Symbols names (edit, delete, content_copy)
    - Action buttons have minimum 44px touch target (min-w-[44px] min-h-[44px] or equivalent padding)
    - Layout uses single responsive path with 12-col grid
    - SwipeToDelete wrapper preserved for mobile swipe behavior
    - Desktop action buttons visible on hover: hidden lg:flex opacity-0 group-hover:opacity-100
  </acceptance_criteria>
  <done>TransactionRow has single responsive path, Material Symbols icons, WCAG-compliant touch targets</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FAB → user input | FAB triggers transaction creation — no input validation needed (button, not form) |
| TransactionRow → user actions | Edit/delete/duplicate are destructive — existing confirmation patterns preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-02 | Tampering | npm/pip/cargo installs | mitigate | No new packages installed — pure refactoring |
</threat_model>

<verification>
1. `grep -c "lucide-react\|bg-emerald\|bg-red-" src/components/ui/fab.jsx` returns 0
2. `grep -c "lucide-react\|function MsIcon" src/components/finance/TransactionRow.jsx` returns 0
3. `npm test` passes
4. `npm run build` succeeds
</verification>

<success_criteria>
- FAB uses semantic color tokens (bg-success, bg-danger) not hardcoded colors
- FAB positioned at bottom-24 right-6 with md:hidden
- TransactionRow has single responsive render path with Material Symbols icons
- Touch targets meet 44px WCAG minimum
</success_criteria>

<output>
Create `.planning/phases/02-ui-consolidation/02-02-SUMMARY.md` when done
</output>
