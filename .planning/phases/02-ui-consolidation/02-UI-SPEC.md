---
phase: 2
slug: ui-consolidation
status: draft
shadcn_initialized: true
preset: new-york
created: 2026-05-19
---

# Phase 02 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized) |
| Preset | new-york style, neutral base color, CSS variables enabled |
| Component library | Radix UI (via shadcn/ui) |
| Icon library | Material Symbols (via MsIcon wrapper) |
| Font | Inter (system-ui fallback) |

**Source:** `components.json` detected at project root. shadcn is fully installed with 50+ components in `src/components/ui/`.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, compact element spacing |
| base | 8px | Default element spacing, vertical rhythm |
| sm | 12px | Tight element grouping, pill padding |
| md | 16px | Standard section gaps, card internal spacing |
| lg | 24px | Page horizontal padding (`px-lg`), section padding |
| xl | 40px | Page vertical padding (`py-xl`), major section breaks |

**Source:** Pre-populated from `tailwind.config.js` spacing tokens and CONTEXT.md decision D-03.

Exceptions: `card-padding: 20px` used for card internal padding (not a standard scale value but already established). `mobile-nav-height: 64px`, `mobile-header-height: 56px`, `sidebar-width: 264px` are layout constants, not content spacing.

---

## Typography

| Role | Size | Weight | Line Height | Token |
|------|------|--------|-------------|-------|
| Display | 22px | 700 | 1.2 | `display-sm` |
| Heading | 18px | 600 | 1.56 (28px) | `headline` |
| Body | 14px | 400 | 1.43 (20px) | `body-lg` |
| Label | 11px | 600 | 1.45 (16px) | `label-caps` |

**Source:** Pre-populated from `tailwind.config.js` fontSize tokens. These 4 roles cover all UI text in the phase.

Additional tokens available but not primary roles:
- `title` (14px/700) — used for card titles, same size as body but bold
- `body-sm` (12px/400) — used for secondary metadata, delta labels
- `display-hero`/`display` (60px/700) — reserved for splash/hero sections, not used in phase 02

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#F8FAFC` (light) / `#0b1326` (dark) | Page background via `--bg-hex` |
| Secondary (30%) | `#FFFFFF` (light) / `#171f33` (dark) | Card surfaces, content areas via `--surface` |
| Accent (10%) | `#005c55` / `#0F766E` | Primary actions, active states, FAB |
| Destructive | `#DC2626` | Delete actions, expense indicators |

Accent reserved for:
- FAB button (`bg-primary`)
- Active nav items in sidebar
- Primary CTA buttons
- Active filter/tab indicators
- Progress bars and accent borders

**Semantic KPI colors** (already established, not to be changed):
- Income: `#10B981` (`kpi-income`) + `#1A5C3A` (`chart-income`)
- Expense: `#EF4444` (`kpi-expense`) + `#991B1B` (`chart-expense`)
- Investment: `#0284C7` (`info`) + `#C2410C` (`chart-investment`)

**Source:** Pre-populated from `src/index.css` CSS custom properties and `tailwind.config.js` color tokens. All colors use CSS variables for dark/light theme switching.

---

## Copywriting Contract

All user-facing text in Brazilian Portuguese (pt-BR).

| Element | Copy |
|---------|------|
| Primary CTA | "Nova transação" (add transaction — the FAB and primary action) |
| Empty state heading | "Nenhuma transacao encontrada" |
| Empty state body | "Adicione sua primeira transacao para comecar a controlar suas financas." |
| Error state | "Algo deu errado. Tente novamente ou recarregue a pagina." |
| Destructive confirmation | "Excluir {entity}: Tem certeza? Esta acao nao pode ser desfeita." |

**Skeleton loading copy:** No text — skeleton blocks are purely visual (gray shapes with shimmer animation). No aria-label changes needed; existing loading state announcements from React Query suffice.

**Source:** pt-BR convention from existing UI (FAB labels: "Nova receita", "Nova despesa"; button labels throughout codebase).

---

## Component Inventory (Phase 02 Scope)

### Components to Modify

| Component | File | Change |
|-----------|------|--------|
| TransactionRow | `src/components/finance/TransactionRow.jsx` | Collapse dual mobile/desktop into single responsive path (D-01, D-02, D-04) |
| FAB | `src/components/ui/fab.jsx` | Migrate lucide-react to Material Symbols; standardize positioning to `bottom-24 right-6`, visibility to `md:hidden` |
| KpiCard | `src/components/dashboard/KpiCard.jsx` | Already unified — no structural changes. Used as-is by Dashboard and Transactions |

### Components to Create

| Component | File | Purpose |
|-----------|------|---------|
| iconMap.js | `src/lib/iconMap.js` | Centralized Lucide to Material Symbols mapping (D-07) |
| DashboardSkeleton | `src/components/dashboard/DashboardSkeleton.jsx` | High-fidelity skeleton for Dashboard page (D-09, D-11) |
| TransactionsSkeleton | `src/components/finance/TransactionsSkeleton.jsx` | High-fidelity skeleton for Transactions page (D-09, D-11) |
| ReportsSkeleton | `src/components/reports/ReportsSkeleton.jsx` | High-fidelity skeleton for Reports page (D-09, D-11) |
| BudgetCard | `src/components/finance/BudgetCard.jsx` | Extracted from Budgets.jsx for Planejamento delegation (D-14) |
| GoalCard | `src/components/finance/GoalCard.jsx` | Extracted from Goals.jsx for Planejamento delegation (D-14) |

### Components to Delete (after migration)

| Component | File | Condition |
|-----------|------|-----------|
| Card (shadcn) | `src/components/ui/card.jsx` | Delete after all Card/CardContent imports removed from pages (Task 4) |

### Vendor Components (DO NOT MODIFY)

All files in `src/components/ui/` that are shadcn vendor code (accordion, alert-dialog, button, calendar, checkbox, command, dialog, dropdown-menu, etc.) keep their internal lucide-react imports. Decision D-08/D-13.

---

## Skeleton Loading Contract

| Page | Skeleton Component | Layout Shapes |
|------|-------------------|---------------|
| Dashboard | `DashboardSkeleton` | 4x KPI card rectangles (200x100px), chart area rectangle (full-width x 300px), section heading lines |
| Transactions | `TransactionsSkeleton` | Filter bar rectangle, 8x transaction row rectangles (full-width x 56px each) |
| Reports | `ReportsSkeleton` | Chart area rectangles (full-width x 250px), stat card rectangles |

**Animation:** Use existing `.shimmer` CSS class (already in `src/index.css` lines 405-414). The shimmer applies a `linear-gradient` sweep at 1.5s ease-in-out infinite.

**Behavior:** Skeletons display only when React Query has no cached data (`isLoading && !data`). Subsequent background refetches use cached data — no skeleton flicker (D-12).

**Base component:** Use existing `shadcn Skeleton` (`src/components/ui/skeleton.jsx`) as primitive. Add `shimmer` class for animation.

---

## Icon Migration Contract

**Scope:** 22 custom component files (non-shadcn) that import lucide-react. 21 shadcn vendor files left untouched.

**Migration target:** All lucide-react icons replaced with Material Symbols via `MsIcon` component (`src/components/ui/ms-icon.jsx`).

**Mapping file:** `src/lib/iconMap.js` — single source of truth documenting every Lucide name to Material Symbols name replacement.

**Common mappings (from codebase analysis):**

| Lucide | Material Symbols | Used in |
|--------|-----------------|---------|
| `Plus` | `add` | FAB, QuickEntry, Budgets, Goals, Planejamento, Settings |
| `Trash2` | `delete` | TransactionRow, Budgets, Goals, Planejamento, Settings tabs |
| `Pencil` | `edit` | TransactionRow, Budgets, Goals, Planejamento, Settings tabs |
| `TrendingUp` | `trending_up` | FAB, Budgets, GlobalSearch |
| `TrendingDown` | `trending_down` | FAB, GlobalSearch |
| `ChevronLeft` | `chevron_left` | CalendarPage, Transactions, Budgets, Settings |
| `ChevronRight` | `chevron_right` | CalendarPage, Transactions, Budgets, Settings, DashSection |
| `Search` | `search` | Transactions, GlobalSearch |
| `X` | `close` | CalendarPage, Transactions, FAB, Settings tabs |
| `Download` | `download` | CalendarPage, Reports, Settings/TabDados |
| `Upload` | `upload` | Transactions, CSVImport, Settings/TabDados |
| `Copy` | `content_copy` | TransactionRow |
| `Check` | `check` | CSVImport, Checkbox (shadcn — leave) |
| `AlertTriangle` | `warning` | Planejamento, Goals, CSVImport, InstallmentConfirm, ColumnMapper |
| `FileText` | `description` | Reports, CSVImport, Settings/TabDados |
| `CreditCard` | `credit_card` | CalendarPage, CSVImport, SuggestionBanner, QuickEntry, TabPersonalizacao |

**After migration:** Run `grep -r "from 'lucide-react'" src/ --include='*.jsx' --include='*.js' | grep -v 'src/components/ui/'` to verify zero custom-code lucide imports remain. Then remove `lucide-react` from `package.json` dependencies.

---

## Page-Level Padding Contract

All 8 pages apply uniform padding: `px-lg py-xl` (24px horizontal, 40px vertical).

| Page | Current Padding | Target |
|------|----------------|--------|
| Dashboard | TBD (audit needed) | `px-lg py-xl` |
| Transactions | `py-xl` (partial) | `px-lg py-xl` |
| Planejamento | TBD | `px-lg py-xl` |
| Budgets | TBD | `px-lg py-xl` |
| Goals | TBD | `px-lg py-xl` |
| CalendarPage | TBD | `px-lg py-xl` |
| Reports | TBD | `px-lg py-xl` |
| Settings | TBD | `px-lg py-xl` |

**Source:** CONTEXT.md decision D-03. No page-type variants — consistent spacing everywhere.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | All 50+ components in `src/components/ui/` | not required |
| Third-party | none declared | not applicable |

No third-party registries declared for this phase. No vetting gate required.

---

## Interaction Contracts

### TransactionRow Responsive Behavior (D-01, D-02, D-04)

| Breakpoint | Layout | Actions |
|------------|--------|---------|
| < md (mobile) | Single-column stacked layout | Swipe-to-delete (existing `SwipeToDelete` wrapper), tap to open edit |
| >= md (desktop) | 12-column grid (current layout) | Visible edit/delete/duplicate buttons on hover; no swipe |

**Touch targets:** All interactive elements minimum 44px (WCAG 2.1 AA). Checkbox, edit, delete, duplicate buttons must meet this.

### FAB Visibility (D-06 standardization)

- Visibility: `md:hidden` only (mobile/tablet)
- Position: `fixed bottom-24 right-6`
- z-index: `var(--z-sidebar)` (50)
- Styling: `bg-primary text-primary-foreground` (teal)

### Skeleton Display Logic (D-12)

```
if (isLoading && !data) → show skeleton
if (isLoading && data)  → show cached data (background refetch)
if (error)              → show error state
if (data)               → show data
```

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
